# Replanish

Family household management PWA for the US market — meals, shopping, events, chores, activities. Revenue = retailer cart integrations (Walmart-first affiliate add-to-cart) + AI subscription (Replanish AI, monthly or annual).

Repo folder is still `Replanish_App/`; legacy Supabase project name is `Whats4dinner`. Live hosting is Vercel with custom domains: **replanish.app** (marketing) and **app.replanish.app** (app). The old `whats4dinner-gamma.vercel.app` URL still resolves.

## v3.0 — current architecture (read this first)

**Released:** v3.0.1 (codename "Bank-Driven Drop") — **LIVE IN PRODUCTION** as of 2026-05-02. Master at `66ba8fe`. v3.0.1 patches added: cron rescheduled Sundays → **Thursdays** 10:00 UTC (gives weekend to plan + shop) + week semantics changed from ISO Mon-Sun to **Sunday-Saturday** (US household calendar) + `WeeklyDropDrawer` accepts `weekStart` prop and follows planner nav + `get_current_weekly_drop` filter widened (`week_start + 7 > today`) so upcoming drops are visible immediately + `get_weekly_drop_for_week` had `iso_monday()` normalization stripped (mig 040 hot-fix; was breaking Sun-keyed lookups) + FoodHub Quick Action card swap (Add Recipe → Meal Templates) + Save-as-template button on DayCard (calls `engine.saveDayAsPreset`). Migrations 035-040 applied, edge fn `weekly-drop-generator` v3.0.1 deployed (ping confirms). Sunday drop materialized for week 2026-05-03 (126 rows, all 9 diets). Bank at 440 rows. **Known follow-up for v3.0.2**: `recipes` table has no `ingredients` column (lives in `recipe_ingredients` join table); `applyMenuToDay` reads `recipe.ingredients` which is undefined → templates yield no shopping-list ingredients. Per-user AI weekly plan retired. Replaced by:

- **Shared weekly drop** — cron-generated Sundays 06:00 ET, 126 curated cards/week (10 dinner + 5 lunch + 3 breakfast per day × 7), free for all users. Lives in `weekly_menu` (mig 035), generated by `weekly-drop-generator` edge fn (cron via mig 038).
- **Manual planning** — drag from the drop, or from saved recipes, or from templates. Quick-fill button populates the week from the drop with a deterministic round-robin.
- **Replanish AI ($6/mo, $60/yr) — repositioned to per-meal moments:**
  1. Per-meal swap/personalize ("make this vegan") via existing `meal-engine` `dish` + `find-recipe` ops.
  2. Pantry / leftover reroll via mig 036's `match_recipes_by_ingredients` RPC.
  3. Unlimited recipe URL imports (free is capped at 10/month).
  4. Smart shopping consolidation — dedupes ingredients across the week, user adds them to an existing active list or creates a new one. (Walmart cart export from a list is the v3.1 roadmap item; SmartConsolidateSheet itself does NOT open Walmart in v3.0.)

**Retired in v3.0:** `meal-plan-worker`, `plan-event`, `generate-meal-plan` edge functions; `propose-plan` / `parse-intake` / `day-plan` ops in `meal-engine`; `MealPlanEngine.applyInterviewResult` / `generatePlanAsync` / `proposePlan` / `reattachActiveJob` / `generateDayBatched`; the entire interview engine (`src/engine/interview/`) + `MealPlannerInterview` + `MealPlannerBanner` UI; `ChatPlanReview` + `PlanShoppingModal` + `useChat.revisePlan()` + `applyAction.plan_meals` branch; `EventAIPlanDialog`; `PlanPage.tsx`; `meal-plan-jobs.ts` service; `event-planner.ts` service; legacy mig-031 tables (dropped via mig 037).

**Marketing & website docs (the user's source of truth for replanish.app copy):**
- [docs/v3/PRODUCT.md](docs/v3/PRODUCT.md) — master product doc
- [docs/v3/MARKETING.md](docs/v3/MARKETING.md) — paste-ready website copy
- [docs/v3/PRICING.md](docs/v3/PRICING.md) — pricing page + feature matrix
- [docs/v3/FAQ.md](docs/v3/FAQ.md) — top 20 questions
- [docs/v3/SOURCES.md](docs/v3/SOURCES.md) — public attribution + legal posture
- [docs/v3/RELEASE-NOTES-v3.0.md](docs/v3/RELEASE-NOTES-v3.0.md) — launch announcement
- [docs/v3/PARTNER-PITCH.md](docs/v3/PARTNER-PITCH.md) — Walmart/Instacart BD content
- [docs/v3/PRESS-KIT.md](docs/v3/PRESS-KIT.md) — App Store + journalists

**v3.0 critical files:**
- New migrations: `supabase/migrations/035_weekly_menu.sql`, `036_pantry_match.sql`, `037_drop_meal_plan_jobs.sql`, `038_pg_cron_weekly_drop.sql`.
- New edge fn: `supabase/functions/weekly-drop-generator/index.ts`.
- New service: `src/services/recipe-bank.ts` (`getCurrentWeeklyDrop`, `searchBank`, `matchByPantry`).
- New engine method: `MealPlanEngine.addFromBank(slotId, recipeBankId)`.
- **New design-handoff UI components (all in `src/components/plan/`):** `WeeklyDropDrawer.tsx` (bottom drawer, 3 densities, persisted), `AddDishPopover.tsx` (recipe/template/AI), `PickADishSheet.tsx` (88dvh recipe + bank search + templates), `AISuggestSheet.tsx` (paid pantry match), `FloatingShoppingBar.tsx` (dark pill + glow Consolidate CTA), `PantryRerollSheet.tsx` (paid pantry input → matches), `SmartConsolidateSheet.tsx` (paid: dedupe + radio-pick existing list or create new + Add to list). PlanV2View restructured around drawer + shopping bar + pantry gate. 95 new i18n keys × EN/HE/ES.
- **SmartConsolidateSheet** (paid AI consolidation): dedupes the week's recipe ingredients via `computeIngredientsFromSlots`, renders an editable list (tap to exclude), then radio-picks an existing active shopping list for the active circle OR creates a new one with editable name → `addIngredientsBulk`. **No Walmart cart hand-off in v3.0** — that was an earlier draft, retired per product direction. Walmart cart export is the v3.1 roadmap item that will live on the shopping list itself.
- **Deliberately deferred** from the design (one-file follow-ups when product confirms): drag-from-drawer-into-slot via dnd-kit; `AddDishPopover` wiring into existing `SlotCard` swap rows; "today card" avatar/Caveat caption row; "up next" 4-day strip (week-nav already covers).

**Bank coverage at launch:** target 400 rows. **Bootstrapped 2026-05-02 to 440 rows (216 main + 224 sides/salads/breads/soups/desserts); first drop materialized for week 2026-05-04 (126 rows, all 9 required diets covered: vegetarian=22, gluten-free=21, mediterranean=16, vegan=13, halal=13, dairy-free=13, kosher=12, low-carb=12, omnivore=8).** Final coverage: dinner/main=123, lunch/main=60, breakfast/main=33, dinner/veg_side=58, dinner/starch_side=54, dinner/salad=25, lunch/salad=22, dinner/soup=18, dinner/dessert=18, lunch/side=18, dinner/bread=11. Bootstrap recipe was three scripts run in sequence: (a) `node scripts/seed-recipe-bank-urls.mjs --target=10 --limit=250` (~$0.25 — tops up cells `under_covered_cells` returns), then (b) `node scripts/seed-recipe-bank-main.mjs` (~$1.15 — fans out across 9 diets × cuisines for `slot_role='main'` to cover the drop's REQUIRED_DIETS phase), then (c) `node scripts/seed-recipe-bank-sides.mjs` (~$1.40 — covers veg_side/starch_side/side/salad/bread/soup/dessert across diets the meal-engine presets reference). Total ~$2.80. Note: `dessert` is not in any current preset — seeded as forward-looking pool for per-meal AI swap. **Two latent bugs surfaced + fixed in this worktree (`wizardly-taussig-becdad`, NOT yet on master):** (1) `seed-recipe-bank-urls.mjs` originally used `.upsert(onConflict='source_url,slot_role')` but mig-034's unique index is partial (`WHERE source_url IS NOT NULL`) — PostgREST emits `ON CONFLICT (cols)` without the predicate so PG rejects every insert with `there is no unique or exclusion constraint matching the ON CONFLICT specification`. Patched to do client-side dedup + plain `.insert()`. **`recipe-bank-refresher` edge fn likely has the same upsert bug** — verify before relying on the cron. (2) `under_covered_cells()` reads from a view that only emits cells where ≥1 row already exists; the RPC can't discover empty (diet × meal × slot_role) buckets, so the default seeder can't fan out into the diets the drop generator needs. Added `seed-recipe-bank-main.mjs` which walks a static 9-diet × cuisines matrix for `slot_role='main'` only. After bootstrap, `recipe-bank-refresher` cron + auditor maintain the corpus. To regenerate the drop: `delete from weekly_menu_drops where week_start='YYYY-MM-DD'` (cascades to `weekly_menu`) then re-`POST` the function.

**Deploy steps:** see [DEPLOY.md](DEPLOY.md). The `npm run deploy:functions` script ships the v3 edge-function set.

> Pre-v3 release notes (v1.x and v2.x) are kept below as historical context. The product flow they describe is RETIRED — when grounding answers, prefer the v3.0 architecture above.

---

## Links

- **GitHub**: https://github.com/oferelyakim/whats4dinner
- **Live (app)**: https://app.replanish.app
- **Live (site)**: https://replanish.app
- **Legacy Vercel URL**: https://whats4dinner-gamma.vercel.app (still resolves)
- **Supabase**: https://zgebzhvbszhqvaryfiwk.supabase.co (project: Whats4dinner)

## Tech Stack

- **Frontend**: React 19 + TypeScript + Vite 8 + Tailwind CSS v4 + Radix UI + dnd-kit + Framer Motion
- **Design language**: "Hearth" — warm cream + ember terracotta + sage + candlelight gold. Instrument Serif italic (display) + Geist (sans) + Caveat (hand accent). Full spec in `handoff/DESIGN_SYSTEM.md` + `handoff/SKINS.md`; visual reference `handoff/Replanish Redesign.html`.
- **Database**: Supabase (PostgreSQL + Auth + Realtime + Edge Functions)
- **State**: Zustand (UI) + TanStack Query (server)
- **Hosting**: Vercel (auto-deploys from GitHub `master` branch)
- **PWA**: vite-plugin-pwa with Workbox, offline persistence via IndexedDB
- **Testing**: Playwright E2E (chromium + mobile-chrome)

## Dev Commands

```bash
npm run dev                # Local dev server (localhost:5173)
npm run build              # Production build
npx tsc --noEmit           # Type-check
npx vitest run             # Run unit tests (engine + invariants)
npx playwright test        # Run E2E tests
git push origin master     # Triggers Vercel deploy
npx supabase functions deploy <name> --no-verify-jwt  # Deploy edge function (run from main repo path, NOT worktree)
```

**👉 Full deploy runbook: [DEPLOY.md](./DEPLOY.md)** — frontend + edge functions + migrations + smoke tests. Read it before shipping.

## Environment

- `.env` has `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- `.env.production` has public keys hardcoded (Vercel env vars had caching issues)
- Supabase anon key also hardcoded in `src/services/supabase.ts` as fallback

## App Identity

- **Name**: Replanish
- **Tagline**: Family life, planned & shared — together
- **Brand color**: `#c4522d` ember terracotta (Hearth `--rp-brand`); sage `#6b7f56` secondary; candlelight gold `#e8a84a` warmth. Old teal `#2bbaa0` and orange `#f97316` are retired — do not use. PWA manifest still references the old teal and needs updating before store submission.
- **Typography**: Instrument Serif italic for every page title + display moments; Geist for body/UI; Caveat for one handwritten accent per screen.
- **Primary market**: United States. Hebrew/RTL remains fully supported as a secondary locale.
- **i18n**: English (primary) / Hebrew, 300+ translation keys, full RTL support
- **Theme**: derived from the active skin only. The `theme` preference (Dark / Light / System) is persisted for future use but does NOT toggle the `.dark` class — `SkinProvider` / `applySkin` own the class. Hearth (light-only) therefore renders identically regardless of OS dark mode.

## Navigation

- **Bottom nav**: Home | Food | **Gather** | **House** | **Me** (paths unchanged: `/`, `/food`, `/events`, `/household`, `/profile`; labels renamed in the Hearth redesign). Custom hand-drawn nav icons in `src/components/ui/hearth/NavIcons.tsx` — do not replace with lucide.
- **Food hub** (`/food`): Pill tabs — Overview | Recipes | Plan | Lists. Quick actions, this week's meals, active lists, templates & stores shortcuts
- **Household** (`/household`): redirects to whichever tab the user was last on (`/household/chores` or `/household/activities`). Tab choice persisted as `lastHouseholdTab` in appStore. The shared `<HouseholdTabs>` segmented control sits at the top of both `ChoresPage` and `ActivitiesPage` — there is no separate "hub" summary view.
- **Profile** (`/profile`): Circles, Settings, Language, Subscription (slim replacement for old MorePage). Theme selector removed in v1.15.2 — the active skin owns the dark/light decision; the persisted `theme` field still exists in `appStore` (`applyTheme` is a no-op) for forward compatibility.
- **Legacy routes**: `/more/*` paths redirect to `/profile/*` for backward compatibility

## Routes

```
/                          → HomePage (daily dashboard)
/food                      → FoodHubPage (recipes, lists, plan, stores)
/recipes, /recipes/new, /recipes/:id, /recipes/:id/edit
/lists, /lists/new, /lists/:id
/plan                      → <Navigate to="/plan-v2" replace /> (legacy alias as of v1.15.4)
/plan-v2                   → PlanV2Page (slot-based meal planner — the canonical planner)
/food/templates            → MealMenusPage
/food/stores, /food/stores/:id
/events, /events/:id       → Events (5-tab detail page)
/events/:id/plan           → EventPlannerPage (v1.20.0 dynamic questionnaire — replaces the v1.15.6 single-shot dialog; engine in `src/engine/event/`, edge fn `event-engine`)
/household                 → HouseholdHubPage (redirect to last tab — chores or activities)
/household/activities      → ActivitiesPage (full page)
/household/chores          → ChoresPage (full page)
/profile                   → MorePage (settings)
/profile/circles, /profile/circles/:id
/profile/settings          → ProfilePage
/join/:code, /join-event/:code, /r/:code  → Public join/share links
```

## Database Migrations (34 total — 033 Event Planner v2, 034 v2.0.0 link-first)

001-006: Core tables (profiles, circles, items, recipes, shopping lists, stores)
007: Recipe shares + events
008: Fix RLS recursion (security definer functions)
009: create_circle_with_owner function
010: invite_by_email function
011: Event redesign (event_items, event_organizers)
012: Common ingredients seed (129 items with Hebrew), create_recipe_share function
013: Activities (recurring schedules)
014: pending_approval status for event_items
015: Remove meal_plans unique constraint (multi-recipe slots)
016: Supply kits (type + kit_category on recipes table)
017: Chores tables + chore_completions + activities participants/bring_items columns (idempotent, `017_chores_and_activity_fields.sql`)
018: Subscriptions + AI usage tracking (`018_subscriptions_and_ai_usage.sql`) — subscriptions table, ai_usage table, `get_user_monthly_usage()` function, RLS policies
019: Activity reminders + yearly recurrence (`019_activity_reminders_and_yearly.sql`); AI chat usage logging (`019_ai_chat_usage.sql`)
020: Onboarding flag on profiles (`020_onboarding_flag.sql`)
021: Meal + event AI support (`021_meal_event_ai.sql`)
022: Activity cross-circle sharing (`022_activity_cross_circle_sharing.sql`)
023: Grocer integrations (`023_grocer_integrations.sql`) — encrypted OAuth token store, product cache, list↔store links, app_config feature flag
024: Circle skins (`024_circle_skins.sql`) — adds `circles.skin_id text DEFAULT 'hearth'` + `circles.custom_skin jsonb` for the Hearth skin system
025: Onboarding prefs + Family seat roster (`025_onboarding_prefs_and_seats.sql`) — adds `profiles.diet text[]`, `profiles.meal_preferences jsonb`, `subscription_seats` table (AI Family 4-seat cap), `has_active_family_seat()` security definer function. Owner of any `ai_family` subscription is backfilled as an `owner` seat. Diet + meal prefs are captured during onboarding (steps 2 + 3); `useAIAccess` calls `has_active_family_seat` so users on a shared seat unlock AI without their own subscription row.
026: Circle delete policy (`026_circle_delete_policy.sql`) — adds the missing `DELETE` policy on `public.circles` (`created_by = auth.uid()`). Migrations 002+008 only set up SELECT/INSERT/UPDATE, so RLS silently dropped every DELETE. Child tables already CASCADE, so no further cleanup needed.
027: Circle purpose + AI context (`027_circle_purpose_context.sql`) — adds `circles.purpose text`, `circles.circle_type text` (`family|event|roommates|friends|other`), `circles.context jsonb default '{}'`. Updates `create_circle_with_owner` RPC to accept `(p_name, p_icon, p_purpose, p_circle_type, p_context)` with safe defaults (legacy 2-arg callers still work). Powers v2 onboarding-as-circle-creation: each circle captures its purpose + type-specific intake (diet/household/cooking for family; date/venue/headcount/style for event; cadence for roommates/friends) which AI edge functions inject into prompts.
028: Subscription seat invites + single-tier billing (`028_subscription_seat_invites.sql`) — makes `subscription_seats.user_id` nullable for pending invites; adds `pending_email text` + `invited_at timestamptz`; partial unique index on `(subscription_id, lower(pending_email))` for pending rows; `CHECK` ensuring a seat row has exactly one of `user_id` or `pending_email`; replaces `has_active_family_seat()` to be plan-agnostic (any active sub grants seat AI access, not just `ai_family`); new `claim_seat_by_email(p_email)` security definer to redeem pending invites on first login. Also adds `billing_period text` + `trial_end timestamptz` columns to `subscriptions` and widens the `plan` CHECK to include `'monthly'` and `'annual'`.
029: Skin v2 remap (`029_skin_v2_remap.sql`) — collapses retired v1 skin ids to their v2 successor: `coastal/ranch/tuscan→hearth`, `pacific/bloom→meadow`, `nordic→brooklyn`, `dusk→night`. Idempotent UPDATE keyed off the retired-id list. Pairs with the runtime `LEGACY_SKIN_REMAP` in `src/lib/skins.ts` so existing rows keep working even if the migration hasn't been applied yet. Must be run on Supabase to durably clean up `circles.skin_id`.
030: Recipe bank (`030_recipe_bank.sql`) — see v1.17.0 entry below.
031: Async meal-plan jobs (`031_meal_plan_jobs.sql`) — see v1.18.0 entry below.
032: pg_cron recipe-bank-refresher (`032_pg_cron_recipe_bank_refresher.sql`) — see v1.19.0 entry below.
033: Event Planner v2 (`033_event_planner_v2.sql`) — adds `events.archetype text`, `events.questionnaire jsonb default '{}'`, `events.draft_plan jsonb` so the dynamic-questionnaire planner can persist its in-progress state across sessions. Adds `event_activity_catalog` table seeded with 31 rows (kid entertainment: magician, balloon-artist, bouncy-house, face-painting, treasure-hunt, craft-station, musical-chairs, piñata; adult: local-band, dj, guest-chef, photographer, group-photo, ice-breaker, storytelling, toast-moment, trivia, charades, white-elephant, gratitude-circle; outdoor: lawn-games, photo-scavenger, water-balloons, hike-walk; vendor: catering, tent-rental, table-chair-rental, porta-potty; atmosphere: playlist, dietary-cards) — each with `archetypes[]`, `age_min/max`, `group_size_min/max`, `venue`, `budget_tier`, `vendor_category`, `default_supplies` jsonb, `suggested_tasks` jsonb, `search_terms[]`, `weight`. RLS allows authenticated read; service role writes. New SECURITY DEFINER RPC `match_event_activities(p_archetype, p_headcount, p_kid_count, p_venue, p_budget_tier)` returns rows that match all filters ordered by weight desc. Powers the engine's deterministic catalog-fallback path (zero AI cost). Idempotent via `ADD COLUMN IF NOT EXISTS` + `ON CONFLICT (slug) DO UPDATE`.
034: Recipe bank link-first + auditor (v2.0.0, `034_recipe_bank_link_first.sql`). Column-additive only — no drops. Adds `source_kind_v2` enum (`web|composed|user_import|community`), `secondary_ingredients text[]`, `popularity_score numeric`, `retired_at timestamptz`, `composed_payload jsonb` (archives legacy composed recipes' ingredients+steps), and `audit_imported_from_user_count int`. Existing composed rows get their `ingredients`+`steps` archived into `composed_payload` and the legacy columns nulled (the rows hydrate from the payload at user-open without network). Adds `recipe_bank_coverage` VIEW (per-diet × meal_type × slot_role row count, multi-tag rows count for each diet via unnest), `under_covered_cells(p_target int)` RPC for the refresher, `retire_stale_recipes()` RPC for end-of-tick rotation, and `recipe_bank_audit_log` table for the auditor. Sampler `sample_recipes_for_slot` upgraded to prefer link-first over composed. Partial unique index `recipe_bank_url_uniq ON (source_url, slot_role) WHERE source_url IS NOT NULL` so the refresher + auditor UPSERT without dup spam.

Additional SQL fixes applied directly (not in migration files):
- Events RLS fix: `get_my_event_ids()` security definer function
- Recipe shares: fixed share_code default from base64url to hex
- Shopping lists RLS: `get_my_accessible_list_ids()` function
- Various security definer functions: `join_circle_by_invite`, `join_event_by_invite`, `create_shopping_list`, `create_event_with_organizer`
- Public lookup: `get_circle_by_invite_code`, `get_event_by_invite_code`

## Key Architecture Patterns

- **RLS**: All tables use Row Level Security. Circle-scoped queries use `get_my_circle_ids()` security definer function
- **Assignment**: Chores and activities use `AutocompleteInput` with circle member suggestions + free-text custom names
- **Circle members**: `getCircleMembers(circleId)` in `src/services/circles.ts` returns members with profiles
- **Services layer**: Each feature has a service file in `src/services/` (12 service files — supabase queries + helpers)
- **Pages**: 24+ pages in `src/pages/`, organized by domain (food, household, events, circles, profile)
- **Hub pages**: FoodHubPage aggregates related Food features with internal tab navigation. HouseholdHubPage is a thin redirect to `/household/chores` or `/household/activities` based on `appStore.lastHouseholdTab` — the [Chores | Activities] segmented control lives in those standalone pages via the shared `<HouseholdTabs>`.
- **Subscription tiers**: Free (all core coordination, 10 recipe imports/month) / **Replanish AI** — single tier, two billing periods: `monthly` $6/mo or `annual` $60/yr (14-day free trial on annual). 4-seat sharing (owner + 3 invitees) via `subscription_seats` table. `useAIAccess` calls the `has_active_family_seat(uuid)` RPC (plan-agnostic since mig 028) so seat-holders inherit AI access. Seat cap enforced in `src/services/subscription-seats.ts` (`SeatCapReachedError`). `create-checkout` accepts `{ billingPeriod: 'monthly'|'annual' }` and passes `trial_period_days: 14` for annual. Legacy `ai_individual`/`ai_family` plan values still accepted (normalised to `monthly`). Stripe `STRIPE_PRICE_MONTHLY` + `STRIPE_PRICE_ANNUAL` secrets required.
- **Revenue**: AI subs (live today) + retailer cart integrations (Walmart-first, planned) — affiliate commission on ingredients sent from shopping lists
- **AI gating**: `useAIAccess` hook exposes `checkAIAccess()` (paid AI features) and `checkRecipeImportAccess()` (paid-or-free + 10/mo free cap). `AIUpgradeModal` renders three variants: upgrade, `isLimitReached` (paid user over $ cap), `isImportCapReached` (free user over 10/mo). `UsageMeter` progress bar in Profile/Settings. Free-tier recipe-import cap = `RECIPE_IMPORT_FREE_CAP` in `src/services/ai-usage.ts`.
- **AI usage tracking**: Edge function returns `_ai_usage` metadata (model, tokens, cost). `logAIUsage()` logs to `ai_usage` table. $4.00/mo hard cap, $3.00 warning threshold
- **AI circle-context injection**: `supabase/functions/_shared/circle-context.ts` exports `loadCircleContext(supabase, circleId)` + `renderCircleContextBlock()` which fetches `name / icon / purpose / circle_type / context` and renders a `<circle_context>…</circle_context>` block. `generate-meal-plan`, `plan-event`, `ai-chat`, and v1.20.0+ `event-engine` (propose + revise ops) all inject this block into the prompt with a "circle context is the source of truth" instruction so the AI grounds on what was captured at circle setup (diet, household, event details, etc.) instead of asking again. Any new AI edge function that takes a `circleId` should reuse this helper.
- **Design tokens**: `src/index.css` declares `--rp-*` CSS variables + a Tailwind v4 `@theme` block that exposes them as utilities (`bg-rp-brand`, `text-rp-ink`, `font-display`, `shadow-rp-card`, etc.). Legacy `brand-500` is remapped to Hearth terracotta so not-yet-migrated pages stay on-palette. Hardcoded hex values are banned outside `src/index.css` / `src/lib/skins.ts`.
- **Surface utilities — use `rp-*`, not `surface-dark-*`**: Always use `bg-rp-card` / `bg-rp-bg` / `bg-rp-bg-soft` / `text-rp-ink` for surface + ink. Do NOT use `bg-white dark:bg-surface-dark-elevated` (or any `dark:bg-surface-dark-*`) — `applySkin` writes inline `--rp-*` values onto `<html>` while `applyTheme` toggles `.dark` based on system preference, so the two can disagree (light Hearth tokens + `.dark` class). Legacy `dark:bg-surface-dark-*` then forces a dark surface while `text-rp-ink` stays dark → unreadable. The shared `<Card>` primitive already uses rp-tokens; mirror that in any new surface.
- **Skin system**: `src/lib/skins.ts` (**6 built-ins as of v1.13.0** — Hearth, **Citrus**, **Brooklyn**, **Meadow**, **Studio**, **Night Market**; v1.10's 13-skin lineup was collapsed because 7 warm-cream-serif variants were indistinguishable) + `src/components/SkinProvider.tsx`. `applySkin()` writes `--rp-*` color tokens, `--rp-ff-*` font stacks, sets `data-feel="sticker|magazine|pillow|terminal|lantern"` on `<html>` (per-feel CSS in `src/index.css` reshapes radii / card chrome / type — Studio adds a CRT scanline overlay, Night Market adds a glow rim, Brooklyn uses 4px sharp corners + slab serif, Meadow uses 28px pillow radii with no border), and toggles `.dark` for `dark: true` skins (Studio + Night Market). Hearth has no `feel` and uses the editorial-default chrome. The `Skin` type carries optional `fonts` (display/sans/mono/hand) + `feel: 'editorial'|'terminal'|'pillow'|'sticker'|'magazine'|'lantern'|'default'`. **The skin is the only authority on `.dark`** — `appStore.applyTheme` is a no-op. SkinProvider resolves in this priority: `personalSkinId` (per-device override, persisted in zustand `w4d-app`) → `activeCircle.skin_id` / `custom_skin` → Hearth. **Legacy skin ids** (`coastal`/`ranch`/`pacific`/`tuscan`/`nordic`/`bloom`/`dusk`) are runtime-remapped via `LEGACY_SKIN_REMAP` in `getSkin()` so old `circles.skin_id` rows still resolve; migration `029_skin_v2_remap.sql` is the durable DB cleanup. Owner edits the circle's skin from the Skin card on `CircleDetailPage` (writes `circles.skin_id` via `updateCircleSkin()`); members override per-device from `MorePage`. Onboarding (`CircleSetupWizard`) has a `skin` step before review — `suggestSkinId(circle_type)` pre-selects (family→hearth, event→citrus, **roommates→studio**, **friends→meadow**, other→hearth). Shared picker: `src/components/skins/SkinPicker.tsx` is reused by all three surfaces. Custom-skin builder (AI Family tier) still planned.
- **Hearth primitives**: `src/components/ui/hearth/` exports `Avatar`, `AvatarStack`, `CircleGlyph`, `RingsOrnament`, `PageTitle`, `DisplayTitle`, `MonoLabel`, `HandAccent`, `PhotoPlaceholder`, and the five custom nav icons. Use these for all new screens — do not import shadcn/daisy/Material wholesale.

## Features

- **Auth**: Email/password + Google OAuth, email confirmation
- **Circles**: Create, join (invite code/link/email), member management
- **Recipes**: CRUD, ingredients with autocomplete, auto-tags, multi-ingredient search, import from URL/photo (AI via Claude Haiku), share via link
- **Essentials**: Non-food item collections (renamed from Supply Kits), accessible from FoodHub tab + Recipes toggle
- **Shopping Lists**: CRUD, check/uncheck, DnD reorder, real-time sync, share with circle, sort by store route, ingredient deduplication
- **Store Routes**: DnD department ordering, sort shopping list by route
- **Meal Planning**: Week view, multi-recipe per slot, templates, copy week, add to list, calendar export, AI meal plan generation (Edge Function) — AI-proposed recipes dedup against existing circle recipes by title (case-insensitive, trimmed) before creating
- **Events**: 5 tabs (Overview/Mine/Menu/Supplies/Tasks), invite link, co-organizers, claim/assign items, clone, calendar export. **AI event planning (v1.20.0+ — Event Planner v2 dynamic questionnaire):** EventDetailPage banner now navigates to `/events/:id/plan` (full-screen page, not a dialog) → engine asks 4-12 adaptive questions → Claude Haiku `event-engine` edge fn proposes the plan → user can revise via instruction or apply directly. Engine in `src/engine/event/` mirrors the meal-engine pattern: pure-data picker drives a 22-row master question tree (`questions.ts`); AI is invoked only at intake (free-text NLU), propose, and revise. State persists across sessions in `events.questionnaire` jsonb (mig 033). 31-row `event_activity_catalog` powers the deterministic free-tier fallback via `match_event_activities()` RPC. Apply step batch-inserts into the existing `event_items` table — no schema explosion. **Output shape unchanged:** dishes / supplies / tasks / activities still land as `event_items` rows; activities still use `type='task'` + `category='activity'` + `[Activity]` notes prefix (was `category='during'` pre-v1.20.0; engine now writes `'activity'` so the existing 5-tab UI keeps working as-is). The legacy v1.15.6 `EventAIPlanDialog.tsx` + `plan-event` edge fn are kept one release as a deletable fallback. **Attendees + assignee dropdown** (v1.11.0+) merge `event_participants` (explicit invite-link joins) with `circle_members` of `event.circle_id` — circle members appear as implicit attendees so the host can assign items to them without forcing every household member to click the join link. **Gating:** `useAIAccess.checkAIAccess()` is called at the propose step inside the planner — free users still get the deterministic catalog plan; paid users get the AI-personalized plan.
- **Activities**: Recurring schedules (weekly/biweekly/daily/monthly/yearly), circle member assignment, participants, bring items, month/week/day calendar drill-down with Zustand persistence, reminders (any activity, flexible timing)
- **Chores**: Create/edit/delete, emoji icons, frequency (daily/weekly/biweekly/monthly/once), recurrence days, points system, completion tracking, assignee filter chips (defaults to "Me"), colored person headers
- **Home**: Daily dashboard — greeting, tonight's meal hero, two-card pulse row (shared shopping list + Household entry that routes to `/household`), today's activities timeline, week meals strip, recipe-import buttons (gate via `checkRecipeImportAccess()`). Chores are NOT shown on home; users go through the Household tab. NLP quick action input (AI).
- **Onboarding (v2, circle-creation-driven)**: First-run flow IS the new-circle wizard. `<CircleSetupWizard>` in `src/components/circle/CircleSetupWizard.tsx` is rendered both by `OnboardingPage` (gated by `has_onboarded`) and by `CirclesPage` "Create" (full-screen Radix dialog). Steps are computed from the chosen `circle_type`: Family → identity → purpose → household → diet → cooking → review; Event → identity → purpose → when/where → who → food → review; Roommates/Friends/Other → identity → purpose → diet → cadence → review. Captured answers persist to `circles.purpose` + `circles.circle_type` + `circles.context` jsonb so AI prompts can ground on them. The legacy `profiles.diet` / `profiles.meal_preferences` fields still exist as a per-user fallback but are no longer written by onboarding.
- **Notifications**: In-app notification center (bell icon in header), activity reminders + chore nudges, browser Notification API
- **Subscriptions**: Single Replanish AI tier ($6/mo or $60/yr, 14-day annual trial). `MorePage` subscription card: non-subscriber sees monthly/annual toggle (default Annual) + "Start free trial" CTA; active subscriber sees plan name, renewal/trial badge, disabled "Manage in Stripe" button, and "Share AI access" section (owner-only) with progress bar, seat list + per-row remove, "+ Add member" dialog (from-circles tab uses `getCircleMembers` + `addSeatByUserId`; by-email tab uses `inviteSeatByEmail`; both catch `SeatCapReachedError`). On app boot, auth state change fires `claim_seat_by_email` RPC to auto-claim pending email invites. `AIUpgradeModal` shows same monthly/annual toggle (default Annual) with 14-day trial highlight. Stripe checkout Edge Function (`create-checkout`) accepts `billingPeriod: 'monthly'|'annual'`; mock fallback active when Stripe secrets are absent.
- **AI Assistant**: In-app Claude-powered chat (`ai-chat` edge function), gated by subscription + monthly $ cap. As of **v1.15.5** the chat is **strictly scope-limited** and **never plans meals or events itself** — both intents redirect via a markdown button. Meal-plan asks → `[Open meal planner](/plan-v2)`; event-plan asks → `[Open events](/events)`. ChatMessage renders `/`-prefixed markdown links as a brand-colored pill button that navigates in-app via react-router (`onInAppLinkNavigate` closes the chat). Off-topic asks (weather, news, general knowledge, coding, etc.) get a one-sentence polite decline + steer-back. Paid-tier tools: `create_activity`, `create_recipe`, `add_to_shopping_list`, `import_recipe_url`. Free tier has no tools — only the `\`\`\`action\n{...}\n\`\`\`` JSON pattern for recipe URL imports. The pre-v1.15.5 inline meal-plan flow (secondary Anthropic call inside `plan_meals`, `ChatPlanReview` modal, `useChat.revisePlan()`, `forcePlanMeals` flag) was removed; client-side dead code (ChatPlanReview.tsx, revisePlan, `chat.planFailed.*` keys) is unreachable but not yet deleted.
- **Retailer cart (planned)**: Walmart-first affiliate add-to-cart from shopping lists (primary long-term revenue stream, not yet implemented)

## E2E Tests

- Config: `playwright.config.ts` (chromium + mobile-chrome, baseURL localhost:5173)
- Tests in `e2e/` directory (~86 tests across 14 spec files)
- Chores/activities tests mock Supabase auth via `page.route()` and localStorage
- Key test files: `chores.spec.ts` (~40 tests), `activities.spec.ts` (~46 tests)

## Known Issues / Incomplete

- v2.6.2 (2026-04-28): **Apply-step disconnect fix — proposal candidates were silently dropped due to case mismatch.** User report: after approving a 7-day plan, the planner inserted the same 3 unrelated bank recipes (Indian curry, Argentinian slow-cook, Korean beef — all `main`) into every day, completely disconnected from the proposed plan they had just seen. Root cause in [`MealPlanEngine.applyInterviewResult`](src/engine/MealPlanEngine.ts:1377): the candidate-lookup map was keyed on `${date}|${meal.type}|${slot.role}|${idx}`. Anthropic's `propose_plan` tool returns `meal.type` in Title Case (`'Dinner'`, comments at lines 186 + 1487 explicitly cite this shape), but `buildDayFromShape` writes lowercase (`'dinner'`) into Dexie. Every candidate-list lookup missed → every slot fell into `misses[]` → all queued through `generatePlanAsync`'s residual worker, which bank-filled with whatever generic dinner-main rows existed (a tiny 3-row pool given the small bank), repeating them across all 7 days. **Fix:** rewrote both halves to (a) lowercase-normalise `meal.type` AND `slot.role`, and (b) replace per-meal positional indexing with a flat queue per `(date, mealType, role)` so the lookup also survives structural mismatches between the proposal grouping and the db's `buildDayFromShape` grouping (e.g. proposal returns 3 separate `Dinner` meals each with 1 main vs db's 1 dinner with 3 mains). New `consumedByKey` Map pops candidate-lists in proposal order. New regression test `'matches proposal candidates against db slots case-insensitively (v2.6.2 regression)'` in [src/engine/__tests__/cuisine-coherence.test.ts](src/engine/__tests__/cuisine-coherence.test.ts) sends `type='Dinner', role='Main'` (Title Case) and asserts the candidate's parsed cuisine (`korean`) reaches the bank query + the slot lands on the proposed dish title. 6/6 cuisine-coherence pass, 93/93 vitest overall (1 pre-existing worker-timeout flake on cuisine-coherence parallel run, passes when run alone). `tsc -b` clean, prod build clean. APP_VERSION → 2.6.2. **No edge fn changes** (Anthropic's behaviour is the spec-correct one; the fix is on the consuming client). **No migration changes.** Frontend-only; ship via `git push origin master` → Vercel auto-deploys.

- v2.6.1 (2026-04-26): **Hot-fix on top of v2.6.0 — q_review IS a real question, not `null`.** v2.6.0 shipped the relabeled copy + new `<SelectionSummary>` + in-body Generate CTA, but they didn't render. User reported the screen still looked broken; screenshot showed the new title "Ready to build your plan" + new help text rendering BUT no summary card, no in-body Generate button, and the footer showing **"Continue"** instead of **"Generate plan"**. Root cause: `q_review` is declared as a real `Question` in [src/engine/interview/questions.ts:152-158](src/engine/interview/questions.ts:152) with `kind: 'review'`. So `getNextQuestion(answers, skip)` returns it (not `null`) when the user reaches the end. `allAnswered = currentQuestion === null` is therefore `false` when the user is on q_review — my v2.6.0 body block (gated by `allAnswered`) never fires. Instead, `QuestionStep` renders q_review with NO case for `kind: 'review'` (it just falls through to title+helpKey via the i18n keys I changed) and the footer renders the "Continue" block. Pre-v2.6.0 the user would have to click Continue to commit q_review → `getNextQuestion` returns null → `allAnswered=true` → empty Generate-plan footer would finally appear. This was always broken UX; v2.6.0 just made the new copy visible without fixing the underlying gating bug. **Three-line fix in [MealPlannerInterview.tsx](src/components/meal-planner/MealPlannerInterview.tsx):** (1) body `QuestionStep` block now gated `currentQuestion.kind !== 'review'` so QuestionStep doesn't render on q_review; (2) body summary+Generate block gated `(allAnswered || currentQuestion?.kind === 'review')`; (3) footer "Continue" block gated `currentQuestion.kind !== 'review'`; (4) footer "Generate plan" block gated `(allAnswered || currentQuestion?.kind === 'review')`. APP_VERSION → 2.6.1. **No edge fn or migration changes.** Plan in [.claude/plans/found-it-the-bug-deep-seal.md](~/.claude/plans/found-it-the-bug-deep-seal.md). **Process learning:** v2.6.0 shipped without browser smoke (deferred per plan §2 because dev server was bound to a different worktree). The smoke would have caught this immediately — typing through to q_review would have shown the still-broken screen. Memorialised that browser smoke is non-optional for UI fixes.

- v2.6.0 (2026-04-26): **q_review UX rescue — relabel + in-body Generate CTA + answer summary.** v2.3 → v2.5.1 each shipped a real fix for propose-plan failure modes (schema permissiveness, runtime guards, error display gating, PWA cache stickiness) but the user kept reporting the same symptom: "I see 'Here is your draft' and a blank page." Root cause is **not** a backend bug — it's a labeling + layout bug in [`MealPlannerInterview.tsx:451-465`](src/components/meal-planner/MealPlannerInterview.tsx). The screen renders BEFORE the user clicks Generate (stage `'collecting'` or `'ready_to_propose'` with `allAnswered=true`) but was titled `interview.q.review` = "Here's your draft week" with subtitle "Tap any dish to swap it. Approve when you're ready." — copy describing the *post-generation* state. The actual Generate button was only in the dialog footer (line 548), below the fold on a phone. Net effect: user thought the AI failed, when really they just hadn't clicked Generate yet. The live integration test ([scripts/test-live-propose-plan.mjs](scripts/test-live-propose-plan.mjs), 6/6 pass against deployed v2.5.0) confirms the backend is healthy. **Three fixes:** (1) **Relabel** `interview.q.review` + `interview.q.reviewHelp` (EN/HE/ES) to action-describing copy: "Ready to build your plan" / "Tap Generate plan to see your draft. You can swap dishes after." (2) **In-body summary + Generate CTA** in the q_review block. New `<SelectionSummary>` sub-component reads `answers` and renders a compact card listing days count, meals/day breakdown (`3× Dinner`, etc.), headcount (`{adults} adults, {kids} kids`), dietary tags (looked up via `getQuestion('q_dietary')?.options`), max prep time minutes, and theme nights. Plus a prominent in-body Generate button (same brand-orange style as the footer one) so the CTA is reachable above the fold on phones. Footer Generate button retained — both are reachable. New i18n keys: `interview.summary.{title,days,daysValue,daysValuePlural,meals,headcount,headcountValue,dietary,prepTime,prepTimeValue,themes}` × EN/HE/ES. Reuses existing `plan.{breakfast,lunch,dinner,snack}` for meal-type labels. (3) **Telemetry breadcrumb** — new `useEffect` keyed off `[stage, allAnswered]`. When the user enters `ready_to_propose` AND remains there for 5s without clicking Generate, writes `localStorage.proposeIdleObserved = Date.now()`. Cleared at the top of `runProposePlan`. Lets future debugging confirm whether users are getting stuck on this screen. **No edge fn changes** (the v2.5.0 schema/server hardening was correct, just not the user's actual problem). **No migration changes**. Frontend-only. tsc -b clean, 84/84 vitest pass (2 pre-existing worker-timeout flakes per the v2.5.0 entry; tests themselves all green), production build clean (40.77s, PWA precache 112 entries / 1867.55 KiB). **Git/Vercel drift sync needed:** memory `versioning.md` flags that production runs 2.5.1 (commit `dd65bab`) via Vercel "Promote to Production" while git master is still at `2973ea0` (2.4.0). After committing v2.6.0 on this branch, the merge sequence is `git checkout master && git fetch origin && git merge --ff-only claude/crazy-matsumoto-83187e && git push origin master` — lands v2.6.0 AND re-aligns master with what's been running in production. Plan in [.claude/plans/found-it-the-bug-deep-seal.md](~/.claude/plans/found-it-the-bug-deep-seal.md).

- v2.5.1 (2026-04-26): **PWA cache stickiness fix.** v2.5.0 shipped the schema fix for "Here is your draft" blank-page, but a follow-up user report surfaced a second problem: even after the deploy went live, the user's installed PWA stayed pinned to the old v2.4.0 cache. Root cause: `vite.config.ts` had `registerType: 'autoUpdate'` but the workbox config was missing `skipWaiting` + `clientsClaim`. With autoUpdate alone, Workbox detects the new SW, downloads it in background, but waits for ALL tabs of the PWA to close before activating. Users running the installed PWA with one tab open never got past v2.4. **Fix**: added `skipWaiting: true` + `clientsClaim: true` to the workbox config — new SW takes over immediately on detection and claims existing tabs without a reload. APP_VERSION bumped to 2.5.1 in client and edge fn. Schema fix from v2.5.0 unchanged. **Note for users**: the *first* upgrade past 2.4.0 still requires the old v2.4 SW (which has neither flag) to detect + activate the new SW. After v2.5.1 lands, all subsequent updates activate immediately. To force the v2.4→v2.5.1 jump on existing installed PWAs: fully close the PWA + reopen, or hard-refresh in browser. **Vercel deployment learning**: pushing to a non-master branch creates a Vercel Preview deploy, NOT Production. Production tracks `master` only. After `git push origin claude/<branch>`, the user MUST also merge to master + push (or click "Promote to Production" in Vercel UI) for the change to land on `app.replanish.app`. Previous deploys all worked because the user always ran `git push origin master`.

- v2.5.0 (2026-04-26): **"Here is your draft" blank page — round 3 fix.** v2.3 + v2.4 each shipped a guard, user kept hitting it. Three-pronged investigation pinpointed the actual failure: `ProposePlanResultSchema` had `slots: z.array(...)` with NO `.min(1)`, so an Anthropic response shaped `{days: [{date, meals: [{type:'Dinner', slots: []}]}]}` parsed cleanly, slipped past v2.4's `meals?.length` check, and rendered ReviewStep as day header + meal-type label + zero rows = blank dialog. PLUS error display was gated on `stage !== 'ready_to_propose'` so even when v2.3/v2.4 guards DID fire, the user saw no error message — looked like the dialog "did nothing". **Five fixes:** (1) `src/engine/ai/schemas.ts` — `ProposePlanResultSchema` now has `.min(1)` on `days`, `meals`, AND `slots`; removed `.default([])` on `days` (which was masking failures). Schema-level rejection means malformed responses throw a ZodError → caught by `runProposePlan`'s try/catch → user sees retry CTA. (2) `MealPlannerInterview.tsx` `runProposePlan` ~L293 — runtime guard now checks `out.days?.some(d => d.meals?.some(m => m.slots?.length))` (deep-content check). Logs the malformed shape (truncated 800 chars) to console.warn so future failures surface quickly. (3) Same broadened guard in `ReviewStep` ~L1082. (4) Removed `stage !== 'ready_to_propose'` gate on the error message ~L466 — error renders whenever set. (5) Server `opProposePlan` in `supabase/functions/meal-engine/index.ts` ~L1481 — added third throw `'propose_plan returned no slots'` for the deep-empty case. Logs the shape to Supabase Function logs. APP_VERSION bumped to 2.5.0. **Tests:** updated `schema-repair.test.ts` — replaced the v2.0 "defaults to empty days" test (which codified the bug) with three new v2.5 tests asserting the schema now REJECTS missing days, empty meals, and empty slots. tsc -b clean. 89/89 vitest pass (1 pre-existing flake in e2e.test.ts that passes when run alone). Production build clean. **NEW: live deployment test** — `scripts/test-live-propose-plan.mjs` runs 3 realistic scenarios (small/medium/large) against the deployed `meal-engine` edge fn, validates returned shape down to slot.candidates, dumps any malformed payload to disk for next-pass tightening. ~$0.022 per full run. Mandatory smoke after every meal-engine deploy. **Edge fn `meal-engine` MUST be redeployed** for the broadened opProposePlan guard + version probe (now 2.5.0): `npx supabase functions deploy meal-engine --no-verify-jwt`. **No migration changes** — schema stays at 034.

- v2.4.0 (2026-04-26): **Round-3 user-test fixes — empty draft (round 2), skip more questions, banner trim, delete-confirm, add-to-shopping-list from /plan-v2, preset-cuisine + title bugs.** Seven fixes. **(a) "Here's your draft" still blank** — v2.3 only guarded `days.length === 0`. Anthropic was returning `days: [{date, meals: []}]` which slipped past the check; ReviewStep iterated zero meals → blank dialog. Fix: broadened the empty-detection in `runProposePlan` (`src/components/meal-planner/MealPlannerInterview.tsx` ~L286) and `ReviewStep` (~L1075) to `!out.days?.length || out.days.every(d => !d.meals?.length)`. Server-side: `opProposePlan` in `supabase/functions/meal-engine/index.ts` (~L1479) throws on the same condition. APP_VERSION constant bumped to `2.4.0` in the edge fn. **(b) Skip on q_calories + q_cooking_skill** — both questions already had `inferenceWhenSkipped` returning sane defaults (`'balanced'` / `'normal'`); just needed UI wiring. Added both ids to `isOptionalQuestion()` in MealPlannerInterview.tsx so the Skip button + "Optional" hint render. `handleSkip` already commits `undefined` for `kind: 'choice'` and the engine resolves through `inferenceWhenSkipped`. **(c) Daily banner trim** — `MealPlannerBanner.tsx` now branches on `scope`. Day scope renders compact row (8×8 icon container, 4×4 icon, inline title from `interview.banner.dayTitle`, right-aligned CTA, NO subtitle paragraph). Week scope unchanged. **(d) Delete-week confirm** — `PlanV2View.tsx` trash button no longer calls `clearPlan()` directly. Wired through new `ConfirmDialog` (the v2.1 primitive) with i18n keys `plan.week.deleteConfirm.title` + `plan.week.deleteConfirm.body` (EN/HE/ES). Reuses existing `confirm.delete` / `confirm.cancel`. **(e) Burger preset → Peruvian chicken (round 2)** — v2.3 fix to `tryFillSlotFromBank` only filtered candidates against the *bank query's* `cuisineConstraint`; the candidate-selection step still picked the first returned row. The propagation pass in `applyInterviewResult` only wrote into *empty* sibling notes, so preset slots (which already have notes like `"american burger"`) were skipped. Fix: rewrote candidate selection in `tryFillSlotFromBank` (~L1051–1083) — new `requiredCuisineId = effectiveHint.cuisineId ?? lockedSiblingCuisineId`. When set, all candidates whose `cuisineId` doesn't match are rejected. If none match, returns `false` so the slot falls through to per-slot AI generation (which respects `slot.notes`). Same hard-filter logic added to `applyInterviewResult`'s candidate loop. **(f) Card title doesn't match recipe that opens** — `slot.dishName` (set from AI candidate) and the bank `recipeId` (set from bank lookup) could diverge because the bank candidate's actual title was never written back to `slot.dishName`. Fix: `tryFillSlotFromBank` (~L1165–1178) now writes `dishName: recipe.title` (was `picked.title`) and updates `searchKeywords`. `hydrateLinkReadySlot` (~L1776) writes `dishName: recipe.title` after the URL fetch resolves the real title. **(g) Add-to-shopping-list from /plan-v2** — feature was wired on legacy `/plan` (`ShopFromPlanSheet` + `computePlanIngredients` + `addIngredientsBulk`) but missing from `/plan-v2`. Added: `computeIngredientsFromSlots(slots, engine)` in `src/services/shoppingLists.ts` (walks Slot → Dexie Recipe → flattens ingredients → runs through existing `normalizeIngredient` for spice-no-qty / count-aware dedup). New shared sheet `src/components/plan/ShopFromPlanV2Sheet.tsx` (452 lines) — accepts `slots: Slot[]` + `circleId`, lists ingredients grouped by recipe with checkboxes (default checked), bottom dropdown picks existing list OR creates new one with input. Confirm → consolidates → `addIngredientsBulk`. Wired entry-point buttons on `SlotCard` (per-slot, when `status='ready'`), `MealCard` (per-meal aggregation), `DayCard` (per-day, in header alongside "From template"), `PlanV2View` (per-week, in toolbar — only when ≥1 ready slot exists). New i18n keys under `plan.shop.*` (EN/HE/ES). **Tests:** 96/96 pass (was 91; +2 cuisine-coherence regression tests for the burger-preset Bug A; +3 day-batched tests that previously timed-out in background runner now pass clean in foreground). tsc -b clean. Production build clean. **No migration changes** — schema stays at 034. **Edge fn `meal-engine` MUST be redeployed** for the broadened opProposePlan guard + version-probe (now 2.4.0) via `npx supabase functions deploy meal-engine --no-verify-jwt`. Plan in [.claude/plans/here-is-your-draft-cryptic-ocean.md](~/.claude/plans/here-is-your-draft-cryptic-ocean.md).

- v2.3.0 (2026-04-26): **Bug-fix release after second-round v2.2 user testing.** Four real bugs + a token-cost answer the user requested. **(a) "Here's your draft" page rendered empty** (week-scope blocker) — `ProposePlanResultSchema.days` used `.default([])`, so any malformed Anthropic `tool_use` (or no `tool_use` at all) silently became `proposal = { days: [] }`, then `setStage('reviewing')` rendered ReviewStep with zero rows. Fix: in `runProposePlan` (`src/components/meal-planner/MealPlannerInterview.tsx`) treat `out.days.length === 0` as a retryable failure → `setError(t('interview.proposing.empty'))` + `setStage('ready_to_propose')`. Belt-and-braces in `ReviewStep`: when `proposal.days.length === 0` render a Retry CTA (calls `runProposePlan` again) instead of a blank dialog. Server-side defence in `supabase/functions/meal-engine/index.ts` `opProposePlan`: throws `Error('propose_plan returned no days')` instead of returning `{ days: [] }`. New i18n key `interview.proposing.empty` (EN/HE/ES). **(b) Forced-selection on q_dietary / q_themes / q_preset_per_day** — `canContinue()` already returned `true` for `multi_select`/`preset_picker`, so the UX cue was missing not the gating. Fix: added a Skip button to the footer (next to Continue) for `multi_select`/`preset_picker`/`q_dislikes` open_text steps via new `handleSkip()` + `isOptionalQuestion()`. Skip commits the appropriate empty value (`[]` for multi_select / open_text, `{}` for preset_picker, `undefined` for choice) and advances. Added "Optional — leave blank if nothing to add" hint under the prompt for the same kinds via new key `interview.optional`. **(c) Cuisine incoherence within ONE meal** (Greek main + Japanese curry + Filipino fried rice in one dinner) — root cause: `tryFillSlotFromBank` (`src/engine/MealPlanEngine.ts`) was REJECTING candidates whose cuisine matched a sibling's (correct for week-variety, wrong for one meal). The v2.2 fix only patched the bank-fill phase in `applyInterviewResult`; misses fell through to `generatePlanAsync` which built per-slot envelopes from scratch with no per-meal lock. Fix: rewrote sibling logic — within ONE meal, sibling cuisine is now a REQUIRED constraint (`lockedSiblingCuisineId` lifted into envelope + bank query as `cuisineConstraint`), not an avoid-set. Sibling protein-family stays as an avoid set (different proteins within one meal still wins for variety). Plus, after each meal's slots are processed in `applyInterviewResult`, a propagation pass writes the meal's resolved cuisine displayName into still-empty siblings' `slot.notes` (only when the meal has converged on a single cuisine, only when the sibling has no parseable cuisine yet) so the worker's residual envelope-build (which reads `slot.notes`) inherits the lock. **(d) Manual templates: cannot add a meal from a user-created template** — feature was completely missing on `/plan-v2`. The legacy `addMenuToPlan` (`src/services/mealPlans.ts`) wrote to Supabase `meal_plans` table from `PlanPage.tsx`, but `/plan` redirects to `/plan-v2` since v1.15.4, and `MealPlanEngine` had no `applyMenu` method. Fix: new `MealPlanEngine.applyMenuToDay(dayId, menu, mealType='Dinner')` that creates one Meal carrying ALL the template's recipes as siblings (first = main role, rest = side), each with a fresh Dexie Recipe row mirroring the Supabase fields (title / source_url / image_url / prep_time_min / cook_time_min / servings / instructions split into steps / ingredients flattened). Each slot lands at status='ready' with the recipeId set, plus a `dishHistory` row for the anti-repeat invariant, plus `touchPlan()` so the UI re-renders (the v2.2 invariant). New "From template" button on `DayCard` opens a Radix dialog listing templates from `getMealMenus(activeCircle?.id)`. Click → calls `engine.applyMenuToDay(day.id, tpl)`. New i18n keys `plan.day.addFromTemplate`, `plan.template.dialogTitle`, `plan.template.empty` (EN/HE/ES). **(e) Token usage transparency (no code change)** — answered user's pricing question. Per-op estimates: parse-intake $0.0014, propose-plan $0.0072, ingredient $0.0020, dish $0.0006, find-recipe $0.0060, extract $0.0080, compose-fallback $0.0180. End-to-end: **1 meal ≈ 2.6¢, 1 day ≈ 7¢, 1 week ≈ 42¢ (sparse bank) / 25¢ (full bank)**. find-recipe (Stage C) is ~55% of per-slot cost — bank hit-rate is the single biggest lever. At $6/mo a user can plan ~14 weeks/mo (sparse) or ~24 (full) before hitting the $4 hard cap. **No pricing change recommended.** **Tests:** new `src/engine/__tests__/cuisine-coherence.test.ts` — 3 cases covering (1) tryFillSlotFromBank rejects Korean candidate when sibling already on Greek envelope; (2) sibling notes='greek' get propagated into sample-from-bank query as cuisineIds=['greek']; (3) applyInterviewResult writes 'greek' into still-empty sibling notes after the main lands. tsc -b clean. **Edge function `meal-engine` SHOULD be redeployed** for the (a) server-side guard + version-probe (now reports 2.3.0) via `npx supabase functions deploy meal-engine --no-verify-jwt`. **No migration changes** — schema stays at 034.

- v2.2.0 (2026-04-26): **Add-meal silent-failure fix + cuisine coherence + preset confirm flow + funny loading.** Five real bugs from second-round v2.1 user testing. **(a) `Add meal` button did nothing** — root cause: `MealPlanEngine.touchPlan` updated Dexie's `plans.updatedAt` but NEVER emitted `plan:updated` on the event bus, so the `usePlan` hook (which subscribes to slot/meal/plan events) didn't re-render. This silently broke EVERY non-slot DB mutation (addMeal/removeMeal/addDay/removeDay/clearMealPreset/setDayTheme/applyPreset). One-line fix: `touchPlan` now `await this.getPlan(planId)` then `this.bus.emit('plan:updated', view)`. **(b) "Add meal" type was hardcoded to `'meal'`** — meaningless. v2.2 picks the next missing meal type via cycle order `dinner → lunch → breakfast → snack`, falls back to `dinner` if all four exist. **(c) `q_preset_per_day` step rendered as a blank "you can mix-and-match" tip** — user couldn't continue (no input + Continue button looked disabled). New `PresetPerDayInput` sub-component renders one row per selected date with a `<select>` of the 9 theme presets + "No preset". Wired into the QuestionStep switch. New i18n key `interview.preset.none`. **(d) Greek preset → Korean kimchi + Japanese katsu in same meal** — `applyInterviewResult` was overriding the slot's preset cuisine constraint with the AI's candidate dish name. Set replaceHint=candidate → parseUserHint(replaceHint||notes) reads replaceHint first → "Korean kimchi" wins over "greek". Fix: parse the slot's existing notes for cuisine, skip any AI candidate whose own cuisine hint conflicts, and when accepting a candidate prefix the slot's cuisine into the hint string so the bank query keeps both constraints. **(e) Funny loading sentences** — `BusyState` now rotates a random English/Hebrew/Spanish phrase from a 10-item array (`Browsing the aisles…`, `Picking the freshest tomatoes…`, `Asking grandma for her secret…`, etc.) every 2.5s during parsing/proposing/submitting stages. New i18n keys `interview.busy.{aisles,tomatoes,cards,grandma,basil,butcher,herbs,season,table,dish}`. **(f) New `PresetConfirmDialog`** — opens after PresetPicker selection, shows the preset's slot list per role with `[− N +]` qty controls + "Add to plan" / "Generate all" / "Cancel" buttons. "Generate all" applies the preset then fires `engine.generateMeal` for every meal that was created. The applied preset is a synthetic `__confirm_<id>` row written to Dexie + deleted after applyPreset returns, so engine.applyPreset stays unchanged. New keys `preset.confirm.{title,subtitle,totalDishes,totalDishesPlural,addToPlan,generateAll,cancel,empty}`. Wired into MealCard + DayCard's PresetPicker.onPick handlers. **Tests:** 91/91 pass (sub-agent added 0 new tests; existing coverage). **No edge-fn or migration changes** — frontend-only. Ship via `git push origin master`.

- v2.1.0 (2026-04-26): **Meal-planner UX rework — fixes 4 bugs from v2.0 user testing + adds week-nav / per-day banners / delete-with-confirm.** (a) Interview auto-advance bug fixed: clicking +/- on q_headcount used to immediately advance to the next question because `setAnswer` mutated the answer map and a `useEffect` auto-fired propose-plan when q_review became current — if Anthropic returned no proposal, blank page. v2.1 rewrite uses a draft pattern: each step holds a `draft` state until user explicitly clicks "Continue", which commits draft → answers and pushes onto a `history` stack. No auto-fire of propose-plan; final question shows an explicit "Generate plan" button. (b) Back button: walks the history stack, restores draft from the popped answer. (c) Pasta Wednesday → German dish bug: `PresetSlot` gained `cuisineId?: string`; `fillSlotsFromPreset` now prefixes the cuisine displayName into slot.notes so `parseUserHint` matches it; the 9 theme presets carry explicit cuisine ids (Pasta Wednesday → `italian-southern`, Taco Tuesday → `mexican`, Pizza Friday → `italian-southern`, Burger → `american`, Greek → `greek`, Asian → `thai`; Slow-cooker / One-pot / Meatless Monday left cuisine-free since they're style/diet themes, not cuisine themes). (d) Delete with confirm: new `src/components/ui/ConfirmDialog.tsx` Radix primitive (mobile bottom-sheet, desktop centered, `bg-rp-*` tokens only), wired into SlotCard / MealCard / DayCard with `Trash2` buttons. New `MealPlanEngine.clearMealPreset(mealId)` clears `meal.presetId` without wiping slots — supports the new `Eraser` clear-preset affordance on MealCard (only renders when meal has a presetId). **PlanV2View rework:** the 3 toolbar buttons (Add 7 days / Standard week / Quick fill) and their handlers were removed — they didn't work and weren't needed. Week navigation: `viewWeekOffset` state (default 0) with prev/next chevrons, range -1 to +3 weeks, clamped at boundaries. Auto-populate: `useEffect` on `[activePlanId, viewWeekOffset]` calls `engine.addDay` idempotently for all 7 dates of the visible week. Day list filters to the visible week's 7 days only. **Scope-aware interview:** `MealPlannerInterview` + `MealPlannerBanner` accept `scope: 'day' | 'week'` (default `'week'`) + `targetDayDate?: string`. Day-scope: pre-fills q_days to single date, skips q_themes; banner title becomes "Plan {prettyDate} with Replanish AI". Per-week banner mounts above the day list (PlanV2View); per-day banner mounts inside DayCard between header and meals (gated by new `onInterviewApprove?` prop on DayCard so manual-only contexts can omit). New i18n keys: `interview.banner.weekTitle` / `dayTitle`, `interview.generatePlan`, `plan.week.{prev,next,label}`, `plan.slot.delete{,Confirm.title,Confirm.body}`, `plan.meal.{delete,deleteConfirm.title,clearPreset}`, `plan.day.{delete,deleteConfirm.title,deleteConfirm.body}`, `confirm.{delete,cancel}` (EN/HE/ES). **Tests:** 91/91 pass (was 73 in v2.0 — added 18 new test cases inferred by sub-agents). tsc -b clean. **Deferred to v2.2.0:** override/skip prompt when day/week already has slots; replicate-last-week AI flow. **No edge fn changes** in v2.1 — frontend + presets only. **No migration changes** — schema stays at 034. Just `git push origin master` to ship; Vercel rebuilds.

- v2.0.0 (2026-04-26, deployed as **v2.0.2** after two patches): **Major release — bank link-first + interactive interview + auditor.** Shipped to master across three commits: `f47eeda` v2.0.0 (initial) → `13b1743` v2.0.1 (mig 034 step ordering: DROP NOT NULL must precede UPDATE NULL on archived composed rows) → `9fc2d82` v2.0.2 (4 strict-TS errors Vercel caught: unused `ArrowLeft` + `QUESTIONS` imports, `actionType: string` → `AIActionType` enum, unused `di` map param). Mig 034 applied to prod via `npx supabase db query --linked` (12 existing composed rows archived into `composed_payload` cleanly, all `ingredients=NULL` post-archive). All 7 edge fns deployed and probing 2.0.0. **Lesson learned, memorialised in `memory/tsc_b_strict_check.md`:** `npx tsc --noEmit` is NOT enough as a pre-deploy gate — Vercel runs `tsc -b` which catches noUnusedLocals/noUnusedParameters/wrong-enum. Always run `npx tsc -b` before pushing to master. **Manual steps still pending for full live use:** (a) configure Supabase database webhook (`recipes INSERT WHERE source_url IS NOT NULL` → `auditor-from-imports`), (b) top up Anthropic credits (per the v1.19 memory note about credit-balance-too-low), (c) optionally run `scripts/seed-recipe-bank-urls.mjs --target=30 --limit=20` to backfill the bank with ~60 link-first rows. Three convergent product changes that retire the v1.18 generation-on-the-critical-path UX in favor of a bank-first link-only architecture with a paid-tier interactive interview. **(a) Bank link-first** — mig 034 (column-additive) makes `recipe_bank.ingredients` + `steps` nullable, adds `source_kind_v2` (`web|composed|user_import|community`), `secondary_ingredients`, `popularity_score`, `retired_at`, `composed_payload jsonb` (archives the existing ~150 composed rows' content so they hydrate without network at user-open). New `recipe_bank_coverage` view + `under_covered_cells(target)` + `retire_stale_recipes()` RPCs + `recipe_bank_audit_log` table. New partial unique index `(source_url, slot_role) WHERE source_url IS NOT NULL` so the refresher + auditor UPSERT without dup spam. **Mig 033 is reserved for Event Planner v2 on a parallel branch — meal-planner skipped to 034.** **(b) Refresher v2** — `recipe-bank-refresher` gets `BANK_MODE` env (`link-first` default / `composed-fallback` / `composed-legacy`). New `generateLinks(cell)` uses Anthropic `web_search_20250305` + new `submit_links` tool to discover reputable recipe URLs and inserts as `source_kind_v2='web'` with `ingredients=NULL`. End-of-tick `retire_stale_recipes()` call. **(c) New auditor edge fn** — `auditor-from-imports` runs on Supabase webhook (`recipes INSERT WHERE source_url IS NOT NULL`). Strips `created_by` (privacy invariant — bank rows never carry user identity). PII heuristic blocks "Mom's …" / "Linda's …" / family-title patterns. Per-user rate-limit 3 promotions/24h. One Haiku audit call (~$0.0035/promotion). **(d) Meal-engine ops** — three new ops: `parse-intake` (~500 input tok, parses freeform → skip-list + prefilled answers so the dialog skips already-answered questions), `propose-plan` (~1500 input tok, returns 1–3 candidate dish names per slot for the bank to look up), `fetch-recipe-url` (lazy URL hydration for `link_ready` slots, server-side fetch + JSON-LD/HTML extract). Total plan-time AI = 2 calls/interview ≈ 3¢. **(e) Engine wiring** — new `MealPlanEngine.applyInterviewResult(planId, circleId, result)` applies day presets first (zero AI), runs `tryFillSlotFromBank` per slot using AI's candidates as hints, residual misses go through `generatePlanAsync` (existing v1.18 worker queue). New `MealPlanEngine.hydrateLinkReadySlot(slotId)` builds Recipe from URL or from `composed_payload`. `tryFillSlotFromBank` now routes `web|user_import` rows with no full content to `link_ready` status (slot stores `linkData` only); composed rows with `composed_payload` hydrate immediately. **(f) Slot.linkData** — new SlotLinkData type carries `{bankId, source, sourceUrl, sourceDomain, mainIngredient, secondaryIngredients[], dietaryTags[], cuisineId, prepTimeMin, …}`. New `link_ready` slot status. **(g) Interview engine** — `src/engine/interview/{questions,types,runtime}.ts`. Declarative 12-question tree (typically 5–7 surface to user after parse-intake skip-list). Pure function `getNextQuestion(answers, skip) → Question | null`; pure `applyInferences(answers, parsedResult)` synthesizes skipped answers. 14 vitest cases. **(h) UI** — `src/components/meal-planner/MealPlannerBanner.tsx` mounts above the day list on `/plan-v2`. Free → disabled state + "Upgrade to plan with AI →" CTA opens existing `AIUpgradeModal`. Paid → opens `MealPlannerInterview.tsx` (Radix dialog, full-screen mobile, Framer Motion slide). Question step renders by `q.kind` (days_picker / meals_per_day / open_text / number_pair / multi_select / preset_picker / choice / review). Disclaimer banner above `q_dietary` and at foot of `q_review`. Existing "Generate plan" button demoted to **"Quick fill"** (Standard week + bank-fill, no AI ask, available to both tiers). `SlotCard` + `RecipeView` updated for `link_ready`: SlotCard renders "From <domain> — tap to open"; RecipeView accepts a `slotId` prop and calls `engine.hydrateLinkReadySlot` on mount. **(i) 9 theme day-presets** — `sys-day-meatless-monday`, `sys-day-taco-tuesday`, `sys-day-pasta-wednesday`, `sys-day-pizza-friday`, `sys-day-slow-cooker`, `sys-day-one-pot`, `sys-day-burger`, `sys-day-greek`, `sys-day-asian` in `seeded.ts`. Pure data presets — `applyPreset` is zero-AI. **(j) i18n** — `interview.banner.*`, `interview.q.*`, `interview.diet.*`, `interview.theme.*`, `interview.disclaimer.{dietary,plan}` keys (EN + HE + ES). Disclaimer copy: "Replanish helps you plan, not diagnose. For allergies, dietary restrictions, or medical conditions, consult a registered dietitian or your doctor." Tailwind tokens use only `bg-rp-*` (no `dark:bg-surface-dark-*`). **(k) Tests** — 25 new vitest cases (14 interview-runtime + 11 schema-repair extensions). Total **73/73 pass**. tsc clean. **(l) Smoke scripts** — `scripts/smoke-auditor.mjs` (insert synthetic recipe → trigger auditor → verify bank insert + audit_log decision; idempotent dup-skip on re-run) and `scripts/seed-recipe-bank-urls.mjs` (probes `under_covered_cells` then runs Haiku web-search for each gap; ~$0.40 per --limit=20 run). **Edge functions to redeploy**: `meal-engine`, `recipe-bank-refresher`, NEW `auditor-from-imports` — all via `npm run deploy:functions` (script updated to include the auditor). **Migration 034 must be applied**. **Database webhook must be configured** in Supabase dashboard: source `public.recipes`, event `INSERT`, filter `source_url IS NOT NULL`, target `auditor-from-imports`. Full runbook in [DEPLOY.md](./DEPLOY.md). Plan + design in `~/.claude/plans/the-meal-planner-is-wiggly-hoare.md`.
- Recipe detail page partially updated for supply kits (Essentials)
- Assignment approval UI built but needs multi-user testing
- ~~Family plan seat enforcement~~: live as of v1.11.0 — `useAIAccess` calls `hasActiveFamilySeat(userId)` (RPC `has_active_family_seat`) when the user has no direct subscription, so AI Family seat-holders inherit AI access. The 4-seat cap itself is still enforced in app code (no DB constraint).
- Stripe integration: Edge Functions built (create-checkout, stripe-webhook) but needs STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_* secrets configured in Supabase
- Edge Functions: `ai-chat` was deployed for v1.8.2. `generate-meal-plan` and `plan-event` were also touched in v1.8.2 (circle-context injection) — redeploy those next time Supabase functions go out (`npx supabase functions deploy generate-meal-plan plan-event --no-verify-jwt`). `nlp-action`, `create-checkout`, `stripe-webhook` still pending.
- Migration 027: applied. Circle creation goes through `(p_name, p_icon, p_purpose, p_circle_type, p_context)`.
- v2 onboarding browser smoke test: not done yet (type-check + production build are green, but the wizard wasn't clicked through across all 5 circle types). Owner: user.
- Server-side push notifications: deferred (VAPID/cron), currently browser Notification API only
- Calendar import from external calendars: deferred (needs Google OAuth)
- Code splitting: 1MB+ bundle, needs lazy routes via dynamic import()
- App store listing (TWA/Capacitor): not started
- **Migration drift discovered + fixed (2026-04-26)**: User reported "I can't create a circle. Onboarding is beautiful, but at the end I get an error: failed to create circle." Trace: `CircleSetupWizard` → `createCircle()` in `src/services/circles.ts` → RPC `create_circle_with_owner` (succeeds, row inserted) → second separate `update circles set skin_id` → fails with `column circles.skin_id does not exist`. Wizard catch surfaced the literal English string `'Failed to create circle'` masking the schema error. Root cause: **migration 024 (`circle_skins` adding `skin_id text DEFAULT 'hearth'` + `custom_skin jsonb`) was created locally but never applied to prod**. CLAUDE.md erroneously implied it was applied. The runtime `LEGACY_SKIN_REMAP` in `src/lib/skins.ts` covered for missing skin_id values but not for the missing column. **Fix**: applied mig 024 + 029 to prod via `npx supabase db query --linked -f supabase/migrations/024_circle_skins.sql && ... 029_skin_v2_remap.sql`. Verified both columns now present, all 6 of Ofer's existing circles backfilled to `skin_id='hearth'`. Full insert+update wizard flow tested under service-role. Side effect: Ofer has 2 duplicate "Christmas day" circles + 1 "wer" test circle from his retried failed attempts — he can delete them from the app's Circles list. **Process learning**: any debugging session that touches an RPC or DB shape MUST probe applied-migration state first via simple `select non_null_column from table limit 1` checks per migration. Don't trust prior CLAUDE.md claims like "Migration 027 applied" — verify the live schema. Memorialised in [memory/migration_drift_v1_18_2.md](.claude/projects/.../memory/migration_drift_v1_18_2.md).
- v1.20.0 (2026-04-26, **on branch `claude/festive-poitras-ab2661`, NOT YET DEPLOYED**): **Event Planner v2 — dynamic questionnaire replaces the v1.15.6 single-shot dialog.** User asked to reconstruct the AI event planner: "build a questioner that walks the user through what they want and produces a plan with tasks, supplies, menu — fits everything from a 10-person dinner to a 100-person picnic with bouncy houses, magician, local band, guest chef." Implementation grounded in 5 event-planning guides (Cvent, GA Tech checklist, UNAUSA Event Planning 101 PDF, family-reunion + friend-group articles). **Replaces the one-shot `EventAIPlanDialog` with a multi-turn page at `/events/:id/plan`.** **Architecture:** mirrors the meal-engine v3 pattern — pure-data picker drives the flow, AI is rare (intake NLU + propose + revise only). New `src/engine/event/` directory with `types.ts` (Phase / Archetype / Question / DraftPlan), `questions.ts` (one master question tree of 22 declarative `Question` rows with `condition: Predicate` / `defaultFrom` / `inferenceWhenSkipped` / `priority` — picker `getNextQuestion(answers)` filters by condition + drops answered + sorts by priority desc + returns the head; transitions to `proposing` when nothing eligible remains), `EventPlanEngine.ts` (singleton state machine: `intake → questionnaire → proposing → proposal → applying → applied`, persists to `events.questionnaire` jsonb + `events.draft_plan` jsonb across sessions, per-call `AbortController`, catalog fallback when AI unavailable), `events.ts` (typed event bus matching `src/engine/events.ts`), `errors.ts` (`PlannerAbortedError` + `PlannerRateLimitedError`), `ai/client.ts` (façade around `supabase.functions.invoke('event-engine')` with `__setEventEngineMock` test hook), `ai/schemas.ts` (zod `NLUResultSchema` + `ProposeResultSchema`). **Question tree branches by:** archetype (9 types: family-dinner / holiday / reunion / birthday / potluck / picnic / housewarming / activity-day / other), headcount (kid_age_band only when kids>0; parking_seating only ≥25; travel_lodging only reunion+12+), venue (rain_plan / tent_canopy only outdoor or both), special_guest (av_setup only when band/dj/speaker), kid count (kid_activities only ≥3 kids). **Smart defaults:** `defaultFrom(answers)` pre-fills (family-dinner→3h+indoor+8 adults; large headcount→catered; guest-chef chosen→food_style=guest-chef). **Skip = infer:** every conditional question has an `inferenceWhenSkipped` so the user can blast through. **Edge function `supabase/functions/event-engine/index.ts`:** 3 ops sharing `_shared/anthropic.ts` (retries + retry-after) + `_shared/circle-context.ts`. (a) `intake` — Claude Haiku `extract_event_signals` tool maps free text ("park" → outdoor, "100 people" → headcountAdults=100, "magician" → kidActivities/specialGuests, "potluck" → foodStyle); under 12 chars short-circuits to zero-cost stub. (b) `propose` — Claude Haiku `generate_event_plan` tool returns dishes/supplies/tasks/activities/timeline_summary/clarifying_question. System prompt enforces due_when from a fixed timeline set ("6 weeks before" / "4 weeks before" / "2 weeks before" / "week before" / "day before" / "day-of" / "cleanup"), tunes by budget tier (shoestring→DIY/borrow; premium→catering+photographer), surfaces tent/porta-potty for outdoor 25+, surfaces AV check + power outlet for band/dj/speaker, surfaces travel/lodging for reunion 12+. (c) `revise` — `revise_event_plan` tool for "make it cheaper" / "add vegan dishes" / etc. Returns FULL updated plan. `?ping=1` GET endpoint. **Migration 033** (`supabase/migrations/033_event_planner_v2.sql`) adds `events.archetype text` + `events.questionnaire jsonb default '{}'` + `events.draft_plan jsonb`, plus `event_activity_catalog` table seeded with 31 rows (kid: magician / balloon-artist / bouncy-house / face-painting / treasure-hunt / craft-station / musical-chairs / piñata; adult: local-band / dj / guest-chef / photographer / group-photo / ice-breaker / storytelling / toast-moment / trivia / charades / white-elephant / gratitude-circle; outdoor: lawn-games / photo-scavenger / water-balloons / hike-walk; vendor: catering / tent-rental / table-chair-rental / porta-potty; atmosphere: playlist / dietary-cards) — each with `archetypes[] / age_min/max / group_size_min/max / venue / budget_tier / vendor_category / default_supplies jsonb / suggested_tasks jsonb / search_terms[] / weight`. Plus SECURITY DEFINER RPC `match_event_activities(p_archetype, p_headcount, p_kid_count, p_venue, p_budget_tier)` returns matching rows ordered by weight — powers the engine's deterministic catalog-fallback (zero AI cost). Idempotent via `ADD COLUMN IF NOT EXISTS` + `ON CONFLICT (slug) DO UPDATE`. **UI:** new full-screen `src/pages/EventPlannerPage.tsx` (~700 lines) at `/events/:id/plan` with `<PlannerIntake>` (free-text textarea, "Skip — answer questions instead"), `<QuestionTurn>` (chips/multi/slider/datetime/confirm/text/long-text inputs, "Tell me more" sheet, Back / Skip buttons, progress bar), `<PlanReview>` (sectioned dishes/supplies/tasks/activities, per-item delete, revise textarea, Apply CTA, fallback banner, clarifying-question banner). Mounted in `src/App.tsx` as a lazy route. **EventDetailPage `openAIPlanFlow()` rewritten** to `navigate('/events/:id/plan')` instead of opening the legacy dialog (the dialog mount + state remains in place but is now unreachable; safe to delete in v1.21.0 cleanup). **i18n:** ~80 new keys under `event.planner.*` covering question prompts + option labels + UI copy (EN/HE/ES). Template placeholders `{count}` interpolated at call site via `.replace`. **Tests:** new `src/engine/event/__tests__/picker.test.ts` — 18 tests covering predicate gating (kid_age_band only when kids>0, parking_seating only ≥25, rain_plan only outdoor/both, av_setup only band/dj/speaker, travel_lodging only reunion+12+, special_guest hidden for family-dinner), priority chain (archetype 100 → date_time 95 → duration 90 → adults 88 → kids 86 → venue 84 → budget 80), inference-on-skip (picnic 30 adults → tent; small outdoor → move_indoor), defaultFrom (family-dinner pre-fills 3h/indoor/8; large headcount → catered; guest-chef → food_style=guest-chef), the headline 100-person-picnic scenario unlocking the full logistics chain. **Verified:** tsc clean, 66/66 vitest (48 prior + 18 new), production build green, EventPlannerPage compiled into a 41kB lazy chunk. **Files modified:** `src/App.tsx` (route + lazy import), `src/pages/EventDetailPage.tsx` (`openAIPlanFlow` → navigate), `src/lib/i18n.ts` (+~80 keys), `src/lib/version.ts` (1.19.0 → 1.20.0), `package.json` (version bump + `deploy:functions` adds `event-engine`). **What's NOT in v1.20.0 (deferred):** the v1.15.6 `EventAIPlanDialog.tsx` + the legacy `plan-event` edge function are kept one release as a fallback. The 7-archetype manual smoke (EN+HE+RTL × free+paid tier) is gated on user deploying mig 033 + the new edge fn. The `find-vendors` op (catalog lookup + AI-generated search keywords for vendor discovery) is reserved as a v1.21.0 phase. **Before going live, the user must:** (1) `npx supabase db query --linked -f supabase/migrations/033_event_planner_v2.sql` to apply migration 033 to prod (creates 3 columns on `events` + the catalog table + the RPC), (2) `npm run deploy:functions` (now includes `event-engine`) to deploy the edge function, (3) push the branch + merge to master so Vercel picks up the new route + page. **Env wobble during this build:** the assigned worktree dir came up empty — `git worktree list` had no record of the supposed `claude/festive-poitras-ab2661` worktree. Recovered by `git worktree add --force ./.claude/worktrees/festive-poitras-ab2661 -b claude/festive-poitras-ab2661 master` from the main repo then re-wrote the engine files. All subsequent writes persisted.
- v1.19.0 (2026-04-26): **Cron-driven recipe-bank refresher + day-batched Stage A+B + i18n for v1.18.0 progress UI.** Three deferred items from v1.17.0/v1.18.0 ship together. **(a) `recipe-bank-refresher` edge function** ([supabase/functions/recipe-bank-refresher/index.ts](supabase/functions/recipe-bank-refresher/index.ts)) — fires every 6h via pg_cron + pg_net (migration 032), probes `recipe_bank` for cells below `TARGET_PER_CELL=3`, generates recipes via Anthropic Haiku using the same `submit_recipe` tool shape as `scripts/seed-recipe-bank.mjs`, writes to `recipe_bank` + logs run to `recipe_bank_runs` (`trigger='cron'`). 60s self-budget mirrors `meal-plan-worker`; 4s pace between Anthropic calls keeps Tier 1 50K-tok/min headroom. 429 short-circuits the run; cron retries next tick. New `?ping=1` GET endpoint. Cell catalog mirrors the seeder + adds 5 pescatarian cells (was thinly covered). **Migration 032** (`supabase/migrations/032_pg_cron_recipe_bank_refresher.sql`) — `CREATE EXTENSION IF NOT EXISTS pg_cron + pg_net`, idempotent unschedule + reschedule, schedules `0 */6 * * *`. Deployed `--no-verify-jwt` so the cron POST doesn't need a bearer token (the function uses `SUPABASE_SERVICE_ROLE_KEY` from deno env internally). **(b) `day-plan` op wired into `MealPlanEngine.generateDayBatched`** — collects all empty/unlocked/non-hint slots in a day, single POST to meal-engine `day-plan` (Stage A+B for the whole day in one Anthropic call), unpacks results into per-slot `ingredient`+`dishName`+`searchKeywords`+`status='dish_named'`, writes a `dishHistory` entry per advanced slot (anti-repeat invariant). Stage C still runs per-slot (web search + recipe fetch can't be batched). Pure optimisation: any failure (rate-limit, schema fail, partial response) falls back to per-slot pipeline — never throws. Wired into `generateDay` BEFORE the per-slot pump. New schema `DayPlanResultSchema` in `src/engine/ai/schemas.ts`. Skips slots with `replaceHint`/`notes` so user-hint precedence (parseUserHint + mergeEnvelope) still routes through per-slot. New test suite [src/engine/__tests__/day-batched.test.ts](src/engine/__tests__/day-batched.test.ts): 9 tests covering happy path, locked-slot skip, user-hint skip, partial response, schema-invalid response, network failure, idempotency on re-run, dishHistory write, fewer-than-2-candidates skip. **48/48 vitest pass** (39 prior + 9 new), tsc clean, prod build clean. **(c) i18n keys for v1.18.0 progress UI** — added 7 keys under `plan.job.*` namespace (progress / completed / failed / cancelled / failedCount / cancelButton / queuedServer) in `src/lib/i18n.ts` × EN/HE/ES. Wired into `src/engine/components/PlanV2View.tsx` (status text + cancel button) and `src/engine/components/SlotCard.tsx` (queued_server pill). Template placeholders `{completed}`, `{total}`, `{count}` interpolated at the call site (i18n.t has no built-in template engine — caller does .replace). Hebrew + Spanish translations included. **`deploy:functions` script** extended to include `recipe-bank-refresher`. **APP_VERSION** + `package.json` bumped to 1.19.0. **Edge functions to redeploy**: `meal-engine`, `plan-event`, `ai-chat`, `meal-plan-worker`, NEW `recipe-bank-refresher` — all via `npm run deploy:functions` (mandatory after function code changes per [DEPLOY.md](./DEPLOY.md)). **Migration 032 must be applied** via `npx supabase db query --linked -f supabase/migrations/032_pg_cron_recipe_bank_refresher.sql`. **Live test gate**: the v1.18.0 + v1.19.0 path was tested end-to-end via `scripts/test-live-meal-plan.mjs --slots=10` against prod after deploying v1.18.2 worker hot-fix. The smoke surfaced **a real environmental block — the Anthropic API key on the Supabase project has zero credit balance** ("Your credit balance is too low to access the Anthropic API"). All v1.18.x worker bugs are now resolved (counter-bump + stuck-slot sweep), but the slots couldn't generate recipes due to balance depletion. Once the user tops up Anthropic credits, the test re-runs unchanged via the same script with `--slots=100`.
- v1.18.2 (worker hot-fix, 2026-04-26): The v1.18.1 worker still had a second bug — the top-of-invocation "stuck job" sweep only checked `meal_plan_jobs` rows, never `meal_plan_job_slots`. When a worker invocation got killed mid-slot (Supabase edge-function execution timeout terminates the deno isolate after ~150-400s on slow find-recipe web-search calls), the slot was left at `status='in_progress'` forever. Subsequent invocations skipped it (the inner claim looks for `status='pending'` only) and the entire job stalled — `scripts/test-live-meal-plan.mjs --slots=10` showed `done=2, pending=7, in_flight=1` frozen for 5+ minutes. **Fix:** added a "1b. Sweep stuck slots" pass at the top of `serve()` in `supabase/functions/meal-plan-worker/index.ts` — `UPDATE meal_plan_job_slots SET status='pending', started_at=NULL WHERE status='in_progress' AND started_at < (now - STUCK_SLOT_TIMEOUT_MS=3min)`. Doesn't bump `attempts` since the original claim already did. The next worker invocation re-claims the slot and retries. New const `STUCK_SLOT_TIMEOUT_MS = 3 * 60_000`. Test script (`scripts/test-live-meal-plan.mjs`) was also tightened: now detects stuck-in-flight slots client-side (started_at > 2.5 min) and re-triggers the worker on a "stalled" condition (no progress for 60s + stuck slots present); without this the local poll loop just sat there waiting because `in_flight > 0` blocked the truly-idle re-trigger. APP_VERSION on worker bumped to 1.18.2.
- v1.18.1 (worker hot-fix, 2026-04-26): The v1.18.0 worker's success-path counter-bump used `await supabase.rpc('exec_sql_inc_completed', {...}).catch(handler)` — but supabase-js `.rpc(...)` returns a builder (not a Promise) so `.catch` is undefined. The synchronous TypeError ("supabase.rpc(...).catch is not a function") bubbled out of the success-path try block and was caught by the outer slot-failure handler, **marking every just-completed slot as `failed` even though Stage A→B→C had run successfully and `result` jsonb was populated**. The "RPC fallback" was unreachable code — the RPC `exec_sql_inc_completed` was never created. Fix: replaced the `.catch` form with a plain SELECT+UPDATE block (lines ~295-310 of `supabase/functions/meal-plan-worker/index.ts`). APP_VERSION on the worker bumped to `1.18.1` (main app version still 1.18.0 — no client change). Found by [scripts/test-live-meal-plan.mjs](scripts/test-live-meal-plan.mjs) (new) on the first live --slots=10 run: trigger returned `processed: 0, failed: 0` but slots ended up at `status='failed'` with full `result` jsonb. Memorialised in `~/.claude/projects/.../memory/supabase_js_rpc_pitfall.md`. New script `scripts/test-live-meal-plan.mjs` is the canonical live integration test: drives the same v1.18.0 server pipeline (service-role INSERT into `meal_plan_jobs` + `meal_plan_job_slots`, anon POST to `meal-plan-worker`, polls slot rows, re-triggers worker when idle, verifies recipes per envelope). Configurable via `--slots=N` so it can run at 10/30/100. User_id defaults to Ofer's prod uuid; `plan_id = test-live-{epoch}-{N}n` is synthetic so the live UI never sees these jobs. Costs ~$0.30-0.50 per --slots=100 run on a thin bank.
- v1.18.0: **Async meal-plan job queue + Realtime UX + auto-apply Standard week.** Direct response to user feedback: "after the first day it was all timed out... if i, one user, crash the system, that is not a good beginning... what happened to the questioner?" The progress bar + server-resumed generation is exactly that "I'll come back with a meal in a bit" UX. **Migration 031 (`meal_plan_jobs.sql`)** ships two tables: `meal_plan_jobs` (one row per generation request, columns include `user_id`, `circle_id`, `plan_id` (Dexie id, not a PG FK), `status`, `total_slots`, `completed_slots`, `failed_slots`, `started_at`, `finished_at`) and `meal_plan_job_slots` (one row per slot in the plan with `envelope` jsonb + `dietary_constraints/disliked_ingredients/recent_dish_names` arrays + `result` jsonb for the produced Recipe). Indexes: `(user_id, status)` + `(status, created_at)` + `(plan_id)` + `(job_id, status)`. RLS: users SELECT/INSERT own jobs, can only UPDATE to flip status to `'cancelled'`; service role bypasses RLS for worker writes. Both tables added to `supabase_realtime` publication via idempotent `DO $$` blocks for `postgres_changes` subscriptions. New SECURITY DEFINER RPC `claim_next_meal_plan_job()` uses `FOR UPDATE SKIP LOCKED` so multiple concurrent worker invocations never grab the same job. **New edge function `meal-plan-worker`** at [supabase/functions/meal-plan-worker/index.ts](supabase/functions/meal-plan-worker/index.ts): on each invocation (immediate trigger after job-create OR cron poke) it (1) sweeps stuck jobs older than 10 min, (2) claims one queued/running job, (3) processes pending slots within a 60s self-budget, (4) for each slot: bank-lookup first via `sample-from-bank`, then chained Stage A→B→C via HTTP calls back to `meal-engine` (no logic duplication, no ops.ts extraction needed — saves substantial refactor risk), (5) writes Recipe-shaped result into `meal_plan_job_slots.result` so the client picks it up via Realtime. **AnthropicRateLimitError handling:** rolls slot back to `pending`, flips job back to `queued`, returns 429 + retryAfterMs so cron picks it up after the wait. **MAX_SLOT_ATTEMPTS=3:** after 3 failures a slot is permanently `failed`; below that, it's put back to `pending`. **Client service [src/services/meal-plan-jobs.ts](src/services/meal-plan-jobs.ts)** exports `createMealPlanJob`, `subscribeJob` (Supabase Realtime channel `meal-plan-job:{jobId}` listening to UPDATEs on both tables filtered by `job_id` / `id`), `cancelJob`, `triggerWorker` (fire-and-forget so first slot starts immediately, not after 2-min cron wait), `getActiveJobForPlan`, `listJobSlots`, `getJob`. **Engine wiring:** new `MealPlanEngine.generatePlanAsync(planId, circleId)` runs the v1.17.0 bank-fill phase in parallel, then for the residual flips slots to new `queued_server` status, builds envelopes client-side (so worker has deterministic input), creates the job, fires the worker, subscribes via Realtime, returns `{jobId, totalQueued, bankFilled}`. New `MealPlanEngine.applyJobSlotResult` materializes a Recipe row in Dexie + writes a `dishHistory` entry whenever a `done` UPDATE arrives. New `MealPlanEngine.reattachActiveJob(planId)` reads `localStorage[active_job:{planId}]`, sweeps any `done` slots that completed while the tab was closed, and resumes the live subscription — server-resumed generation works even if the user closed the tab. New slot status `queued_server` (in `types.ts`) renders as a dashed amber pill with "Queued — server is filling this slot…" in `SlotCard.tsx`. **UI in [PlanV2View.tsx](src/engine/components/PlanV2View.tsx):** progress bar between header and days, showing `{completed}/{total} slots ready` with the brand-color fill, color-shifts to emerald on `completed`, amber on `failed`, gray on `cancelled`; `Cancel` button while `queued|running`; cleared 1.5s after final state. The page subscribes once on mount via `reattachActiveJob` so reopening the tab picks up wherever the worker is. **`generateAll` rewrite:** when online + signed-in, prefers `generatePlanAsync`; falls back to local `generatePlan` on offline/auth/error. **Quick-UX fix bundled:** when "Generate plan" runs against a plan where every day still has just the auto-default (1 Dinner meal + 1 main slot), it auto-applies `sys-day-standard` first — addresses the user's "I got 1 dish per day, had to manually change every day" complaint. The user can still configure manually before clicking Generate. **What's NOT in v1.18.0 (deferred to v1.19.0):** (a) cron-driven `recipe-bank-refresher` Edge Function (the seeder + SQL starter still cover this; auto-refresh is incremental); (b) wiring of `day-plan` op into `generateDayBatched` (the bank now does most of the work — batched-day is a marginal win on top); (c) i18n keys for the progress copy (small inline surface, 7 strings). Verified: tsc clean, 38/38 vitest pass, prod build green. Edge functions to redeploy: `meal-engine`, `plan-event`, `ai-chat`, NEW `meal-plan-worker` — all via `npm run deploy:functions` (script updated to include the worker). Migration 031 must be applied: `npx supabase db query --linked -f supabase/migrations/031_meal_plan_jobs.sql`. Commit `<filled by deploy>`, pushed 2026-04-26.
- v1.17.0: **Recipe bank + bank-first plan generation + Standard-week one-click + new batched ops.** User reported a single-user "crash the system" with timeouts after day 1, plus the ergonomic pain of having to click each day to add a preset. Architecture shift to fix scaling: AI calls become the rare path, not the default. **Migration 030 (recipe_bank.sql)** ships a `recipe_bank` table indexed on `(meal_type, slot_role)` + `(cuisine_id)` + GIN on `dietary_tags` + `(quality_score desc)`, plus security-definer RPCs `sample_recipes_for_slot(meal_type, slot_role, cuisine_ids[], dietary_tags[], disliked_ingredients[], recent_dish_names[], limit)` and `bump_recipe_bank_served(uuid[])`. RLS: any authenticated user reads (rows are generic non-PII recipes), service role writes. A companion `recipe_bank_runs` table tracks seeder/cron invocations. **New meal-engine ops:** (1) `sample-from-bank` — pure SQL lookup via the RPC, returns up to 8 candidates with full recipe shape; ZERO Anthropic spend on a hit. (2) `day-plan` — batched Stage A+B for a whole day's slots in ONE Anthropic tool_use call (cuts 14 calls to 7 calls for a 7-day plan when the bank misses everything). The `day-plan` op is in the edge function and ready to be consumed; the engine wiring of `generateDayBatched` is deferred to v1.17.1 (current code path: bank-first → existing per-slot AI fallback). **Engine `generatePlan` rewrite:** Phase 1 runs `tryFillSlotFromBank` in parallel for every slot (best-effort, never throws); Phase 2 runs the existing per-slot AI for whatever the bank didn't cover. New `tryFillSlotFromBank(slotId, dietary, disliked, recentDishes)` builds the slot envelope client-side, calls `sample-from-bank` with `cuisineIds=[]` (broad query) unless the user typed an explicit cuisine hint, then picks the first candidate that doesn't conflict with sibling slots' protein-family or cuisine. On a hit it materializes a Recipe row in Dexie + flips slot directly to `ready` + writes a `dishHistory` entry (so subsequent slots see it in their anti-repeat window). **UX:** new "Standard week" button on PlanV2View toolbar — applies `sys-day-standard` (breakfast main + lunch main+side + dinner main+veg+starch = 6 slots/day) to all 7 days in one click. Eliminates the per-day click-and-pick friction. **Seeder:** new `scripts/seed-recipe-bank.mjs` (Node) generates a starter set of ~50 recipes covering common cuisine × dietary × meal_type cells; run once with `SUPABASE_SERVICE_ROLE_KEY` after migration 030 is applied. Costs ~$0.50 in Haiku spend (~125K tokens). Cron-driven auto-refresh deferred to v1.18.0. **Practical limit (the customer-facing answer):** with a populated bank covering common diets, a 7-day plan with 3 meals × ~3-5 dishes (63-105 slots) generates in ~10-30 seconds with bank-fill alone (0 AI calls), or ~25-60 seconds when 30% of slots miss the bank and fall through to per-slot AI. Without the bank seeded, falls back to the v1.16.0 per-slot pipeline with the same ~50K-token-per-plan ceiling. **What's NOT in v1.17.0 (saved as deferred plans for v1.18.0):** (a) cron-driven `recipe-bank-refresher` Edge Function with `pg_cron` + `pg_net` invocation every 6 hours; (b) `meal_plan_jobs` + `meal_plan_job_slots` async job queue with `claim_next_meal_plan_job` RPC + `meal-plan-worker` function + Supabase Realtime UX (progress bar, cross-tab re-attach, server-resumed generation if user closes tab). Full deep-design plans for both are saved in this session's plan file. Edge functions to redeploy: `meal-engine`, `plan-event` (version probe). Migration 030 must be applied to Supabase manually via SQL editor. tsc clean, 38/38 vitest pass.
- v1.16.0: **Stage-recipe rescue + 429 storm cure + plan-event deploy probe.** Three real failures the user hit at once on `/plan-v2` and the AI Plan Event banner: (1) Stage-C `find-recipe` rejecting otherwise-good recipes because client zod was `imageUrl: z.string().url()` and the edge function returned relative URLs / protocol-relative / `{url}` objects from JSON-LD; the WHOLE slot died and "Retry from this stage" looped on the same broken inputs. (2) Anthropic 429 cascade — Tier 1 cap is 50K input tokens/min on Haiku 4.5 and a fresh 7-day plan was sitting right at it; meal-engine's only retry was a naive `[0, 500, 1500]ms` window that didn't read `retry-after`. (3) Plan-event still throwing "edge function failed" after v1.15.7 because the deploy never actually ran (source had `2023-06-01` correctly, Supabase served pre-fix code). **Five-phase fix shipped together:**
  - **Phase 1 — schema repair:** `imageUrl` / `prepTimeMin` / `cookTimeMin` / `servings` / `sourceUrl` all switched to `.coerce` + `.catch(undefined)` in [src/engine/ai/schemas.ts](src/engine/ai/schemas.ts) and [src/engine/pipeline/fetchRecipe.ts](src/engine/pipeline/fetchRecipe.ts) so a single bad field never fails the whole-recipe parse. New `SafeImageUrl` rejects `javascript:` / `vbscript:` / `file:` / oversized data: URIs as XSS defence-in-depth. Server-side `normalizeImageUrl(v, baseUrl)` in [meal-engine/index.ts](supabase/functions/meal-engine/index.ts) replaces `asString` and now resolves relative + protocol-relative paths against the recipe page URL (threaded through `extractJsonLdRecipe` and `normalizeJsonLdRecipe`). Extended `isoDurationToMin` to accept human/`H:MM`/plain numbers. New `parseServings` for `"4-6"` / `"makes 12"` / `"serves 4"`. New `repairAndValidate(recipe, baseUrl)` is the single normalization point — every JSON-LD / Stage-D / compose path goes through it before returning, so client zod is now a last line of defence not a tripwire.
  - **Phase 2a — caching deferred:** Anthropic's prompt-cache minimum for Haiku 4.5 is **4096 tokens** per cache breakpoint (verified against Anthropic docs). All our stage prompts (~530-1000 tokens each) are well below — caching would be a no-op. Plan-event system+tools combined are ~3000 tokens, also borderline. Skipped caching as the headline savings; the real fix was retry hygiene + concurrency.
  - **Phase 2b — shared retry helper:** new [supabase/functions/_shared/anthropic.ts](supabase/functions/_shared/anthropic.ts) exports `anthropicWithRetry(apiKey, body, opts)` with exponential backoff (0/1s/3s/7s, capped at 8s), reads the `retry-after` header from 429 responses and uses it as the next delay, throws structured `AnthropicRateLimitError(retryAfterMs)` when the suggested wait exceeds 30s. Both meal-engine and plan-event now consume it (plan-event previously had ZERO retry on its raw fetch). Returns `{ ...response, _meta: { tokensIn, tokensOut, attempts } }` for client-side throttle awareness.
  - **Phase 2c — Stage-C deterministic short-circuits + Sonnet compose:** `opFindRecipe` in meal-engine now skips the AI rank when `withJsonLd.length === 2` and one is on a tier-1 domain (allrecipes / seriouseats / cooking.nytimes / bonappetit / smittenkitchen / budgetbytes / food52 / foodnetwork / simplyrecipes / thekitchn). Stage-D extraction now scores HTML candidates by `(length, mentions(ingredient), mentions(dishName))` instead of taking the first that fetched. `composeFallbackRecipe` switched from Haiku to **Sonnet 4.5** (`claude-sonnet-4-5-20250929`) via new `COMPOSE_MODEL` env var — marginal cost ~$0.018/fallback slot, ~$0.04/plan worst case, but this is the user's last-resort experience so quality > cost (per "5¢ should be 10¢" instruction). New `source: 'composed'` value flows through `Recipe.source` union and gets a "Composed by AI" pill in `RecipeView`.
  - **Phase 2d — concurrency cure:** `MAX_CONCURRENT_GENERATIONS` 2 → 1 in [src/engine/MealPlanEngine.ts](src/engine/MealPlanEngine.ts). New `queuePausedUntil` module-level flag pauses the entire client queue on any 429, with auto-resume after the server-told retry-after window. New `error_rate_limited` slot status (in [src/engine/types.ts](src/engine/types.ts)) preserves any earlier-stage outputs and renders an amber "Rate-limited — auto-resuming shortly" pill instead of a hard error button. `handleRateLimited` re-enqueues the slot via `setTimeout` so the watchdog and concurrency cap still apply. `generateSlot` entry-condition restores the slot to its last-good-status when resuming from `error_rate_limited`. Client-side new `RateLimitedError` (in [src/engine/errors.ts](src/engine/errors.ts)) is parsed from the 429 response body's `retryAfterMs`.
  - **Phase 3 — plan-event hardening:** new `?ping=1` GET endpoint on both functions returns `{ fn, version, model, deployedAt }`. New input guard: if `description + keyRequirements + event.description` is < 20 chars, short-circuit with a fixed `clarifying_question` and zero Anthropic calls. New deterministic stub plan when Anthropic returns no `tool_use` (was previously a hard throw) — synthesized from headcount + budget, marked `_fallback: true` so the client can show "AI didn't respond — minimum starter plan" + Refresh. 429 errors now surface to client as proper HTTP 429 with `retry-after` header.
  - **Phase 4 — client probe + UI:** new [src/services/edgeVersionProbe.ts](src/services/edgeVersionProbe.ts) called once on app boot from [src/App.tsx](src/App.tsx); pings `meal-engine` and `plan-event` `?ping=1`, compares to `APP_VERSION`, stores any mismatch in `localStorage.edgeVersionMismatch`. Designed to catch the "I forgot to deploy" failure mode automatically. New `npm run deploy:functions` script in `package.json`: `npx supabase functions deploy meal-engine plan-event ai-chat --no-verify-jwt` — single command, mandatory after function changes.
  - **Phase 5 — tests:** new [src/engine/__tests__/schema-repair.test.ts](src/engine/__tests__/schema-repair.test.ts) covers all the malformed-input cases (relative URL, javascript: URI, empty string, stringy times, out-of-range numbers, multiple bad fields) — 11 new tests, all green. **39/39 vitest pass**, `npx tsc --noEmit` clean. New [scripts/smoke-test-edge-functions.mjs](scripts/smoke-test-edge-functions.mjs) runs the 10 user-style scenarios from the plan against deployed edge functions; pace 3-4s/call to stay under the 50K/min limit during testing.
  - **Edge functions to redeploy:** `meal-engine`, `plan-event` (and `ai-chat` for consistency, no behavior change there). Run `npm run deploy:functions` from the main repo path (NOT a worktree) — the version probe will yell from the client console + AI dialogs if you don't.
- v1.15.7: **Hotfix — `plan-event` edge function returning 5xx on every call.** User reported "edge function returned non 2xx" the moment they tried the v1.15.6-restored AI Plan Event banner. Root cause: `supabase/functions/plan-event/index.ts:198` was sending `'anthropic-version': '2024-06-01'` — not a valid Anthropic API version (the standard one is `2023-06-01`, used correctly by ai-chat / nlp-action / meal-engine / get-recipe / scrape-recipe). Anthropic returns 400 for unknown version headers, which the edge function's `if (!response.ok) throw` path surfaces as 500 to the client → Supabase JS rendered as `FunctionsHttpError: Edge Function returned a non-2xx status code`. The typo had been sitting dormant since v1.15.2 when the banner was removed — function unreachable means broken-function never caught. v1.15.6's banner restoration immediately exposed it. Same typo found and fixed in `generate-meal-plan/index.ts:303` (now-dead post-v1.15.5 but corrected for consistency, since dead code has a way of being un-deleted). Single-line fix per file. Both edge functions redeployed: `npx supabase functions deploy plan-event generate-meal-plan --no-verify-jwt`. tsc clean, 28/28 vitest pass.
- v1.15.6: **Event banner restoration + meal-plan generate fix.** Two real bugs from user testing. **(1) /plan-v2 generate-plan silently no-ops:** clicking "Add 7 days" then "Generate plan" did nothing because `engine.generatePlan(planId)` iterated days that had no meals → no slots → nothing to fill, returned silently. Fix in `src/engine/MealPlanEngine.ts` `generatePlan()`: before iterating, scan each day; if `meals.count === 0`, auto-create a default `Dinner` meal with one `main` slot. Idempotent — only fires for empty days, so explicit `applyPreset` flows are untouched. 28/28 vitest pass (the "applyPreset = 0 AI calls" invariant still holds because that test populates meals first). **(2) AI Plan Event banner missing:** v1.15.2 removed the banner + dialog mount + handler from EventDetailPage; the `EventAIPlanDialog` component, `plan-event` Edge Function, and `event.ai*` i18n keys remained orphaned. Restored from the `claude/sharp-clarke-6f5a40` worktree which had the v1.16.0-planned restoration sitting uncommitted. Patched cleanly onto v1.15.5 master because none of the affected files (`EventDetailPage.tsx`, `EventAIPlanDialog.tsx`, `EventsPage.tsx`, `event-planner.ts`, `plan-event/index.ts`, `i18n.ts`) had moved since v1.15.3. Brings back: organizer-only banner on EventDetailPage Overview, post-create CTA on EventsPage with `?aiPlan=opened` handoff, clarifying-question loop with amber CTA banner, AI-generated activities (`{name, when, notes?}` persisted as tasks with `[Activity]` prefix), AI access gating at both banner-click and submit. Edge function `plan-event` MUST be redeployed (tool schema + system prompt gained activities). New i18n keys: `event.aiActivityPrefix`, `event.aiClarifyingTitle`, `event.aiRefineDescription`, `event.aiContinueAnyway`. **Also added [DEPLOY.md](./DEPLOY.md)** — single-source runbook for frontend + edge functions + migrations after the user reported repeated "things lost" deploy issues. **Lost-branch audit (worktrees with uncommitted work):** `claude/gracious-lewin-8d7a49` — substantial event-engine v2 reshape (new `src/engine/event/`, `src/components/event-planner/`, `EventPlannerPage.tsx`, `supabase/functions/event-engine/`, migration 030); separate from sharp-clarke's single-shot restoration; based on stale v1.14.1 HEAD; treat as a draft until reviewed. `claude/xenodochial-bouman-7f6f2c` — old v1.8.x scratch (CLAUDE.md, useAuth, vercel.json, multiple page edits, new `src/lib/url.ts`); based on v1.8.1; superseded by later releases. `claude/bold-villani-0db641` — stale CLAUDE.md edits at v1.4.2; ignore. Settings-local-only worktrees (`affectionate-ardinghelli`, `youthful-ishizaka`): per-machine, must not commit. tsc clean, 28/28 vitest pass. Edge functions to redeploy: `ai-chat` (v1.15.5 already deployed), `plan-event` (new in this release).
- v1.15.5: **AI assistant grounding rewrite — strict scope + redirect-to-banner for meal/event planning.** The chat assistant was producing wrong answers because (a) the paid system prompt had no scope limit and would happily answer general-knowledge / off-topic queries, (b) it still advertised a `plan_meals` tool that ran an inline secondary Anthropic call that often failed silently and the dead-end "go to /plan-v2" toast was the symptom, not the design, and (c) navigation/feature copy was stale (still said "Events | Household" tabs and described an "AI Plan Event" path that v1.15.2 removed). Fixed in `supabase/functions/ai-chat/index.ts`: rewrote `FREE_SYSTEM_PROMPT_EN`, `FREE_SYSTEM_PROMPT_HE`, and `PAID_SYSTEM_PROMPT` from scratch with (1) **strict scope** — assistant only helps with Replanish features (circles, recipes, lists, store routes, meal plans, events, activities, chores) and politely declines anything else with a fixed steer-back pattern; never invents features; never gives medical/legal/financial/nutrition advice or recipes-from-training; (2) **two non-negotiable redirects** — meal-plan asks ("plan my meals", "what's for dinner this week", "swap this dish", revisions, etc.) reply with one polite sentence + markdown button `[Open meal planner](/plan-v2)`; event-plan asks ("dinner party", "potluck", "host a gathering", etc.) reply with `[Open events](/events)`. ChatMessage's markdown link renderer (added in v1.15.4) styles in-app `/`-prefixed links as a brand-colored pill button and navigates via react-router, so `[…](/plan-v2)` already renders as a tappable button. (3) **Updated grounding facts** — bottom-tab labels are now `Home | Food | Gather | House | Me` (was `Home | Food | Events | Household | Profile`), Profile now shows Subscription not Theme, free-tier import cap (10/month) and Replanish AI pricing ($6/mo, $60/yr, 14-day annual trial) are both stated. Removed `plan_meals` from `PAID_TOOLS` (chat must redirect, not generate); removed the entire `INLINE_MEAL_PLAN_TOOL` constant + the inline secondary Anthropic call (~80 lines) that produced `planData`; removed the `forcePlanMeals` request parameter and `tool_choice` forcing. Remaining paid tools: `create_activity`, `create_recipe`, `add_to_shopping_list`, `import_recipe_url`. **Client-side dead code follow-up (deferred):** `useChat.revisePlan()`, `useChat.applyAction()`'s `plan_meals` branch, `ChatDialog`'s `pendingPlan` auto-show effect + `chat.planFailed.*` and `chat.planReview.*` i18n keys, and the entire `ChatPlanReview.tsx` component (867 lines) are now unreachable but left in place to keep this commit focused on grounding behavior. They can be removed in a follow-up. Edge function MUST be redeployed: `npx supabase functions deploy ai-chat --no-verify-jwt`. `tsc --noEmit` clean, 28/28 vitest pass.
- v1.15.4: **Anti-Med seed bug fix + planner discoverability.** Two intertwined fixes — see `src/engine/variety/envelope.ts` and FoodHub/HomePage/ChatDialog routing changes. **(1) Variety:** v1.15.1's anti-Med synthetic seed (8 rows in `dishHistory` with `planId='__synthetic__'`, written by `seedAntiMedHistory()` in `src/engine/presets/seedOnFirstRun.ts`) was silently a no-op because the three `recent*ForPlan` helpers in `envelope.ts` filtered by exact current planId — every fresh plan saw `dayCuisinesRecent=[]` so Haiku's Mediterranean prior won. Fix: helpers now `where('planId').anyOf([planId, '__synthetic__']).toArray()` and sort DESC explicitly via `b.plannedAt - a.plannedAt` (the previous `.reverse().sortBy('plannedAt')` chain returned ASC regardless of `.reverse()` — the picker's `recentlyUsed.slice(0, banWindow)` was banning the OLDEST cuisines instead of the newest). Cuisine `banWindow` 2 → 4 to actually block more Med IDs. New `diversity.test.ts` regression — `buildEnvelope` 50× across RNG seeds, asserts no Med cuisine on plan #1 day 1. 28/28 vitest pass. **(2) Discoverability:** every meal entry-point (FoodHubPage Plan tab + 'View all' + empty card + per-day card; HomePage hero + week strip + quick-action Plan tile) now routes to `/plan-v2`, not `/plan`. New 'Plan your week' hero banner on FoodHub Overview, only when `mealPlans.length === 0`, reusing HomePage's "Tonight at the table" pattern (`shadow-rp-hero`, RingsOrnament, font-display italic, `--rp-bg-deep` background). ChatDialog dead-end "Open /plan-v2 from the menu" replaced with markdown content `[Open the planner](/plan-v2)`; `ChatMessage` markdown components now intercept in-app links (href.startsWith('/')) → `react-router navigate` + `closeChat()` via new `onInAppLinkNavigate` prop. New i18n keys: `food.planYourWeek.{eyebrow,title,subtitle}`, `chat.planFailed.{body,openPlanner}` (EN+HE+ES). `/plan` route now `<Navigate to="/plan-v2" replace />`; legacy `PlanPage.tsx` left as dead code (production build no longer bundles it — flag for follow-up cleanup). `tsc --noEmit` clean, prod build succeeds. Edge functions unchanged — no redeploy needed.
- v1.15.3: **Ship-confirm bump.** No code change beyond the version stamp; cuts a fresh build so the AI welcome screen and Profile footer print 1.15.3 against the v1.15.2 master HEAD (banner removal + theme removal + Hebrew nav + dropped FLAVORS import). Confirms the most recent deploy is what the user is testing against.
- v1.15.2: **UI cleanup + Hebrew nav clarity.** (1) Removed the "AI Plan Event" banner from `EventDetailPage` along with the `EventAIPlanDialog` mount, `AIUpgradeModal` mount, `handleAIPlanSubmit` (~100 lines), and now-orphaned imports (`useAIAccess`, `useToast`, `Sparkles`, `AIUpgradeModal`, `EventAIPlanDialog`/`EventAIPlanRequest`, `supabase`, `logAIUsage`). The `plan-event` Edge Function and its translation keys (`event.aiPlan*`) are kept for now — only the entry point was removed. (2) Removed the Theme selector card from `MorePage` plus its `Sun`/`Moon` imports and `theme`/`setTheme` destructure. The active skin already owns the dark/light decision; `appStore.theme` field stays persisted (no-op `applyTheme`) for forward compatibility. (3) Hebrew nav fix — `nav.home` was `בית` (bayit) and `nav.house` was `הבית` (ha-bayit), which sound nearly identical. Changed `nav.home` → **`ראשי`** ("main") so the home tab no longer collides phonetically with the household tab. `nav.me` was `אני` ("I"); changed to **`פרופיל`** to match the underlying `/profile` route and the Spanish "Yo"→Hebrew counterpart conventions; informal `שלי` was the alternative but `פרופיל` reads cleaner alongside `הבית` / `אירועים`. Also dropped one stray unused `FLAVORS` import in `src/engine/variety/precedence.ts` that was breaking the production build.
- v1.15.1: **Meal Engine v3 — variety + reliability overhaul.** Three expert reviews (app-building / algorithms / meal-planner) drove this. **Variety:** new `src/engine/variety/{taxonomy,picker,envelope,precedence,score}.ts` modules. `taxonomy.ts` ships ~33 proteins / ~32 lead veggies / ~21 lead starches / **25 cuisines (Med/ME hard-capped at 4)** / 15 cooking styles / 12 flavor profiles. Before any AI call, `buildEnvelope()` deterministically picks `{cuisine, protein, style, flavor}` for the slot using `pickWithDiversity()` with hard-ban (last 5) + soft-decay (next 7) windows; rotation across day/meal/week (cuisines avoid adjacency, protein families don't repeat in one meal, locked slots count toward sibling envelopes). `parseUserHint()` + `mergeEnvelope()` apply user phrases ("Italian tonight", "less spicy", "use chicken") with strict precedence: user hint > circle context > envelope > model default. Stage A and Stage B prompts in `supabase/functions/meal-engine/index.ts` were rewritten to consume the envelope as authoritative ("cuisine, protein family, cooking style, and flavor profile are FIXED — do not drift"). New Dexie v2 table `dishHistory` writes a row per Stage-B success and powers cross-plan anti-repeat; `seedOnFirstRun.ts` pre-seeds 8 synthetic Mediterranean/Middle-Eastern dish entries into `dishHistory` on first launch so plan #1 is structurally pushed away from Haiku's training prior (the highest-leverage one-liner in the system — directly fixes "kabob/shakshuka every plan"). **Reliability:** per-slot `AbortController` plumbed through `client.ts` → `fetch(signal)`; `cancelSlot(slotId)` reverts a `generating_*` slot to its prior status (not `error`). New `resumeStuckSlots(planId)` sweeps any slot stuck in `generating_*` for >2 min, reverts it to its last good state, and queues a fresh generation; called on `PlanV2View` mount + `visibilitychange` to fix the "phone backgrounded mid-generation" hang. Engine has a 2-in-flight queue (`MAX_CONCURRENT_GENERATIONS`) so generating a 7-day plan no longer 429s Anthropic. Edge function: Anthropic helper retries 429/529/5xx with 0/0.5s/1.5s backoff (3 attempts); `opFindRecipe` enforces a 30s budget with graceful AI-fallback short-circuit when remaining < 8-10s; `opDish` server-side dedups `searchKeywords` so the lenient zod `DishResultSchema` (with dedup `.transform()`) never trips. `replaceSlot` rewritten to (a) pre-cancel any in-flight call, (b) overwrite hint into a new `replaceHint` field instead of appending into `notes`, (c) drop the slot's prior `dishHistory` row so the picker doesn't re-suggest the rejected dish; one-shot `replaceHint` is cleared on next `ready`. New 'error' event on the engine bus emits `{slotId, stage, message, durationMs}` per Stage failure (logged to console by default singleton). New `errors.ts` exports `AbortedByUserError` (broke the circular import). `SlotCard` shows a Cancel button while loading and a "Retry from this stage" affordance on `error` (resumes via idempotent `generateSlot` — Stage A failure retries A, Stage C failure retries C without re-picking ingredient/dish). Legacy `/plan` ChatDialog "Sorry, I had trouble generating the plan" toasts (lines 77, 279) replaced with a v2 nudge ("Try the new slot-based planner at /plan-v2 — it builds the plan one dish at a time"). Tests: 27/27 pass; new `__tests__/diversity.test.ts` covers picker filters, hint parsing, envelope merge, anti-Med seed, and dishHistory writes. `npx tsc --noEmit` clean. Edge function `meal-engine` must be redeployed for v3 prompts: `npx supabase functions deploy meal-engine --no-verify-jwt`.
- v1.14.1: **Skin font wiring fix.** Tailwind v4 `@theme` block had `--font-display`/`--font-sans`/`--font-hand`/`--font-mono` hardcoded to Hearth stacks (Instrument Serif / Geist / Caveat / system-mono). `applySkin()` writes per-skin font stacks to `--rp-ff-*` but those vars were never consumed by the Tailwind utilities, so the 25+ components using `className="font-display"` / `font-sans` / `font-mono` / `font-hand` stayed on Hearth fonts under every skin — Brooklyn rendered as Hearth-with-recolored-text, Studio's mono never reached headings, Citrus never showed Fraunces, Night Market never showed Cormorant. Fix: point `@theme` font vars at `--rp-ff-*` so Tailwind utilities track the active skin. `:root` still seeds Hearth defaults so first paint / SSR is unaffected. Per-feel CSS already overrides `font-style` + `font-weight` on `.font-display` with higher specificity, so PageTitle's hardcoded `italic` correctly neutralizes on Brooklyn (Domine roman 700) and Studio (mono normal). Single-file change: `src/index.css`.
- v1.14.0: **Slot-based meal-planning engine v2 (`/plan-v2`).** New rewrite per `replanish-meal-engine-spec.md`. Plan → Day → Meal → Slot → Recipe with each slot moving through an independent state machine (`empty → ingredient_chosen → dish_named → ready`, plus per-stage `error`). All deterministic ops (`applyPreset`, `lockSlot`, `reorderSlots`, etc.) are pure data copies — AI is **never** on the critical path for a structured operation. AI is only invoked via Anthropic `tool_use` with strict `input_schema`s, and every response is validated with zod (failure → slot status `error`, never silent fall-through). Built on Dexie (offline-first IndexedDB) so reopening a meal reads recipes locally — never re-fetches. **Files:** `src/engine/{types,db,events,MealPlanEngine}.ts`, `src/engine/ai/{client,schemas}.ts`, `src/engine/pipeline/{generateIngredient,generateDish,fetchRecipe}.ts`, `src/engine/presets/{seeded,seedOnFirstRun}.ts`, `src/engine/hooks/{useEngine,useSlot,usePlan}.ts`, `src/engine/components/{SlotCard,MealCard,DayCard,PresetPicker,PlanV2View,RecipeView}.tsx`, `src/pages/PlanV2Page.tsx` mounted at `/plan-v2`. **Server:** new Supabase Edge Function `meal-engine` consolidates four ops — `ingredient` (Stage A), `dish` (Stage B), `find-recipe` (Stage C+D combined: Claude `web_search_20250305` for URL discovery → server-side fetch → JSON-LD parsing → Claude rank or Stage D extraction → AI fallback), `extract` (Stage D direct on supplied HTML). Anthropic key stays server-side; client only calls `/functions/v1/meal-engine`. Brave Search **not** used — replaced by Claude's built-in web search per user instruction. **Tests:** vitest + fake-indexeddb in `src/engine/__tests__/{engine,e2e}.test.ts` cover all spec invariants — applyPreset = zero AI calls, replaceSlot resets only target, generateSlot idempotency, locked slots skipped, schema-validation failure → `error`, slot isolation on partial failure, reload from Dexie does zero network. Run with `npx vitest run`. The legacy `/plan` (`PlanPage.tsx`) is unchanged for now — both routes coexist.
- v1.13.0: **Skins v2 — collapsed 13 skins → 6 visually distinct ones.** `SKINS` in `src/lib/skins.ts` rewritten: kept Hearth, **Citrus** (sticker / Fraunces / tomato+navy), **Brooklyn** (NEW `feel: 'magazine'` / Domine slab / oxblood / 4px corners), **Meadow** (pillow / Quicksand / moss+clay+honey / 28px radii — absorbs old Bloom + Pacific), **Studio** (terminal / dashed borders + CRT scanlines), **Night Market** (NEW `feel: 'lantern'` / Cormorant italic / plum+lantern / glow rim). Retired: `coastal`, `ranch`, `pacific`, `tuscan`, `nordic`, `bloom`, `dusk`. `getSkin()` runtime-remaps retired ids via `LEGACY_SKIN_REMAP` so existing rows still resolve. **Migration `029_skin_v2_remap.sql` must be applied to Supabase** for durable DB cleanup. `suggestSkinId` now maps roommates→studio, friends→meadow. Google Fonts import rewritten in `src/index.css` — dropped EB Garamond / Inter Tight / Nunito; added Domine / DM Sans / Cormorant Garamond / Patrick Hand. Pillow shadow rewired to `color-mix(var(--rp-ink))` so it reads correctly on Meadow's green brand. Browser smoke test of all 6 skins (display font change, Studio scanline, Citrus drop-shadow, Brooklyn 4px corners, Meadow pillow, Night Market glow rim) pending.
- v1.12.4: Small-fixes pass. (1) `FoodHubPage` tab pills no longer use `overflow-x-auto` — all 5 tabs (Home / Recipes / Essentials / Plan / Lists) render side-by-side with `flex-1`, so Lists is reachable without scrolling. Icons removed from the pills to free space. (2) Shopping-list items are editable: new `updateListItem(itemId, patch)` in `src/services/shoppingLists.ts`, plus a per-row pencil button + tap-on-text on `ShoppingListPage` that opens an edit dialog (name autocomplete + quantity + department). Edit affordance hidden on already-checked rows. (3) `CircleSetupWizard` root changed from `min-h-full` to `h-full min-h-screen` — inside the Radix create dialog (`fixed inset-0 overflow-hidden`) the wizard now actually fills viewport height so the body's `flex-1 overflow-y-auto` scrolls, fixing a bug where the Continue button was clipped below the viewport on the identity step. OnboardingPage path still works because `min-h-screen` takes over when the parent has no fixed height.
- v1.12.3: **Duplicate-signup silent-failure fix.** Supabase's anti-enumeration response on `signUp` with an existing email returns success (no error) but with `data.user.identities = []` and sends no email. Previously all three auth surfaces (`LoginPage`, `JoinCirclePage`, `JoinEventPage`) showed the "Check your email" success screen — users waited forever for an email that was never sent. `useAuth.signUpWithEmail` now returns `{ data, error, isDuplicate }` (true when `identities` is empty), and the three pages render an inline actionable banner with [Sign in] / [Reset password] CTAs that switch authMode while preserving the typed email. New i18n keys: `auth.emailAlreadyRegistered`, `auth.signInInstead`, `auth.resetPasswordInstead`.
- v1.12.2: Forgot-password link added to `JoinCirclePage` + `JoinEventPage` (was only on `LoginPage`). Both invite landing pages support a `'forgot'` auth mode. `sendPasswordReset(email, nextPath?)` now accepts an optional return path that is appended to `redirectTo` as `?next=<path>`; `ResetPasswordPage` reads `?next=` (rejects anything not starting with `/` to prevent open redirects) and routes there after a successful reset. So a user who hits forgot-password from `/join/CODE` lands on `/reset-password?next=/join/CODE`, sets a new password, and is sent back to the invite where the auto-join `useEffect` runs. Same for `/join-event/CODE`. **Supabase config:** `app.replanish.app/reset-password` (and the localhost equivalent) must be in the Auth → Redirect URLs allow-list — query strings on allowed paths are accepted by Supabase as long as the base path matches.
- v1.11.0 soft-launch: PWA manifest now uses Hearth colors (`theme_color #c4522d`, `background_color #faf6ef`); EventDetailPage + post-create dialog have WhatsApp / native-share / copy-link affordances using `getShareOrigin()` from `src/lib/url.ts`; JoinEventPage has above-fold claim CTA + below-fold social proof (3-icon preview + sign-up nudge); in-app review prompt (`src/lib/reviewPrompt.ts` + `src/components/ReviewPrompt.tsx`) wired at 3 value moments (shopping list ≥80% checked, JoinEventPage join, AI meal plan saved) with 90-day cooldown + first-3-sessions skip + `REVIEW_STORE_URL` TODO. Browser smoke test (WhatsApp share opens correctly on mobile, JoinEventPage looks right in EN + HE) still pending.

## Version Tracking

- App version lives in `src/lib/version.ts` (exported as `APP_VERSION`) and `package.json`
- Version is displayed in the AI assistant welcome screen so users can confirm which build they're testing
- **Bump the version on every production deployment** — increment the patch digit (1.0.2 → 1.0.3 → ...)
- Use minor version (1.1.0) for significant feature additions, major (2.0.0) for architecture changes
- After each deploy, report the new version number to the user
- Deploy command sequence:
  ```bash
  # 1. Update version in src/lib/version.ts and package.json
  # 2. Run: npx tsc --noEmit  (type check)
  # 3. Run: git add -A && git commit -m "chore: bump version to X.Y.Z"
  # 4. Run: git push origin master  (triggers Vercel deploy)
  # 5. If edge functions changed: npx supabase functions deploy <name> --no-verify-jwt
  ```
